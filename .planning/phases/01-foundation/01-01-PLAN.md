---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lobster-tui/internal/chat/content.go
  - lobster-tui/internal/chat/content_test.go
  - lobster-tui/internal/chat/model.go
  - lobster-tui/internal/chat/views.go
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04]

must_haves:
  truths:
    - "Protocol table messages arrive at View() as BlockTable structs (not flat strings)"
    - "Protocol code messages arrive at View() as BlockCode structs (not markdown fences in a string)"
    - "Streaming text followed by a structured block produces both blocks without data loss"
    - "Existing renderMessage still works via Content() backward-compat helper"
  artifacts:
    - path: "lobster-tui/internal/chat/content.go"
      provides: "ContentBlock sealed interface + 5 block types + Content() helper"
      contains: "type ContentBlock interface"
    - path: "lobster-tui/internal/chat/content_test.go"
      provides: "Unit tests for content block model"
      contains: "TestContentBlock"
    - path: "lobster-tui/internal/chat/model.go"
      provides: "Updated ChatMessage struct, protocol handlers, stream flush"
      contains: "Blocks []ContentBlock"
    - path: "lobster-tui/internal/chat/views.go"
      provides: "Updated renderMessage using Content() helper"
      contains: "Content()"
  key_links:
    - from: "lobster-tui/internal/chat/model.go"
      to: "lobster-tui/internal/chat/content.go"
      via: "ChatMessage.Blocks field uses ContentBlock types"
      pattern: "Blocks.*ContentBlock"
    - from: "lobster-tui/internal/chat/views.go"
      to: "lobster-tui/internal/chat/content.go"
      via: "renderMessage calls msg.Content() for backward compat"
      pattern: "msg\\.Content\\(\\)"
    - from: "lobster-tui/internal/chat/model.go protocol handlers"
      to: "lobster-tui/internal/chat/content.go block types"
      via: "TypeTable/TypeCode/TypeAlert/TypeAgentTransition create typed blocks"
      pattern: "Block(Table|Code|Alert|Handoff)"
---

<objective>
Replace ChatMessage's flat `Content string` field with a typed `[]ContentBlock` array so protocol messages (table, code, alert, handoff) survive to View() as structured data instead of being serialized into markdown strings. Add backward-compatible `Content()` helper method so existing rendering code continues to work.

Purpose: Foundation for Phase 3's native renderers -- typed blocks enable lipgloss/table rendering instead of glamour markdown mangling.
Output: content.go with sealed interface + 5 block types, updated ChatMessage struct, updated protocol handlers, Content() helper, and tests.
</objective>

<execution_context>
@/Users/tyo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tyo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lobster-tui/internal/chat/model.go (CURRENT -- will be modified):
```go
type ChatMessage struct {
    Role    string  // "user", "assistant", "system", "handoff"
    Content string  // FLAT STRING -- this gets replaced with Blocks
    Agent   string
}

type ActiveComponent struct {
    Component biocomp.BioComponent
    MsgID     string
    CreatedAt time.Time
}
```

From lobster-tui/internal/protocol/types.go:
```go
const (
    TypeText            = "text"
    TypeCode            = "code"
    TypeTable           = "table"
    TypeAlert           = "alert"
    TypeAgentTransition = "agent_transition"
    TypeDone            = "done"
)

type TablePayload struct { Headers []string; Rows [][]string }
type CodePayload struct { Language string; Content string }
type AlertPayload struct { Level AlertLevel; Message string }
type AgentTransitionPayload struct { From string; To string; Reason string }
```

From lobster-tui/internal/chat/views.go:
```go
func renderMessage(msg ChatMessage, styles theme.Styles, width int, mdRenderer *glamour.TermRenderer, inline bool) string
// Currently accesses msg.Content directly
```

Key model.go sites that must change:
- Line 115-122: ChatMessage struct definition
- Line 154: streamBuf *strings.Builder (used for streaming text accumulation)
- Lines 662-698: TypeText/TypeMarkdown/TypeCode protocol handlers (write to streamBuf as flat strings)
- Lines 929-938: TypeTable handler (serializes to ASCII art via renderProtocolTable)
- Lines 782-791: TypeAlert handler (creates ChatMessage with Content string)
- Lines 703-717: TypeAgentTransition handler (uses recordHandoff which creates ChatMessage)
- Line 1421-1430: appendMessage/appendMessages
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create content block model with tests</name>
  <files>lobster-tui/internal/chat/content.go, lobster-tui/internal/chat/content_test.go</files>
  <behavior>
    - TestContentBlock_BlockTypes: Each of the 5 types (BlockText, BlockCode, BlockTable, BlockAlert, BlockHandoff) satisfies ContentBlock interface
    - TestContentHelper_TextOnly: ChatMessage with [BlockText{"hello"}, BlockText{" world"}] returns "hello world" from Content()
    - TestContentHelper_Mixed: ChatMessage with [BlockText, BlockCode, BlockTable, BlockAlert, BlockHandoff] concatenates correctly -- code gets fenced, table gets ASCII via renderProtocolTable(headers, rows, 80), alert/handoff get plain text
    - TestContentHelper_Empty: ChatMessage with no blocks returns ""
    - TestFlushStreamBuffer: flushStreamBuffer on non-empty streamBuf appends BlockText to last assistant message's Blocks; on empty streamBuf does nothing
    - TestFlushBeforeTypedBlock: Simulates text in streamBuf, then TypeTable arrives -- both BlockText and BlockTable end up in Blocks in correct order
  </behavior>
  <action>
Create `content.go` in `internal/chat/` package with:

1. **Sealed ContentBlock interface** with unexported `blockType() string` method:
   - `BlockText{Text string}`
   - `BlockCode{Language string, Content string}`
   - `BlockTable{Headers []string, Rows [][]string}`
   - `BlockAlert{Level string, Message string}`
   - `BlockHandoff{From string, To string, Reason string}`

2. **Content() method on ChatMessage** that concatenates all blocks into a flat string for backward compat:
   - BlockText: append Text directly
   - BlockCode: wrap in triple-backtick fence with language
   - BlockTable: call `renderProtocolTable(v.Headers, v.Rows, 80)` (existing function in views.go)
   - BlockAlert: append Message
   - BlockHandoff: append Reason
   - Use strings.Builder for efficiency

3. **appendBlock() helper method on *Model** that appends a ContentBlock to the last assistant message, or creates a new assistant message if none exists.

4. **flushStreamBuffer() method on *Model** that:
   - If streamBuf is empty, returns immediately
   - Otherwise creates BlockText{Text: streamBuf.String()}, resets streamBuf
   - Appends the BlockText to last assistant message's Blocks (or creates new message)

Write tests FIRST (RED), then implement (GREEN). Tests go in `content_test.go`.

Note: renderProtocolTable is defined in views.go and already exists -- the Content() helper can call it. The function signature is `func renderProtocolTable(headers []string, rows [][]string, maxWidth int) string`.
  </action>
  <verify>
    <automated>cd /Users/tyo/Omics-OS/lobster/lobster-tui && go test ./internal/chat/ -run "TestContentBlock|TestContentHelper|TestFlush" -count=1 -v</automated>
  </verify>
  <done>
    - content.go exports ContentBlock interface and 5 concrete block types
    - Content() method produces backward-compatible string output
    - flushStreamBuffer() correctly handles empty/non-empty cases
    - All 6+ test cases pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate ChatMessage and protocol handlers to use typed blocks</name>
  <files>lobster-tui/internal/chat/model.go, lobster-tui/internal/chat/views.go, lobster-tui/internal/chat/content_test.go</files>
  <action>
This task modifies existing files to use the content block model created in Task 1. The migration must be backward-compatible -- all existing rendering continues to work via Content() helper.

**Step 1: Update ChatMessage struct** (model.go line ~115):
- Remove `Content string` field
- Add `Blocks []ContentBlock` field
- Keep `Role` and `Agent` unchanged
- The Content() method already defined in content.go replaces the field

**Step 2: Fix all `msg.Content` field accesses** across model.go and views.go:
- Every `.Content` field read becomes `.Content()` method call
- Every `.Content =` field write becomes `.Blocks = []ContentBlock{BlockText{Text: value}}`
- Use grep to find ALL sites: `msg.Content`, `.Content =`, `.Content +=`, `.Content ==`
- Key sites in views.go: renderMessage accesses msg.Content -- change to msg.Content()
- Key sites in model.go: appendMessage sets Content, streaming uses streamBuf -> Content

**Step 3: Update protocol handlers** in model.go to create typed blocks:

a) **TypeCode** (line ~687): Instead of writing markdown fences to streamBuf, do:
   ```
   m.flushStreamBuffer()
   m.appendBlock(BlockCode{Language: p.Language, Content: p.Content})
   ```

b) **TypeTable** (line ~929): Instead of renderProtocolTable + appendProtocolCodeBlock, do:
   ```
   m.flushStreamBuffer()
   m.appendBlock(BlockTable{Headers: p.Headers, Rows: p.Rows})
   ```

c) **TypeAlert** (line ~782): Instead of `ChatMessage{Content: p.Message}`, do:
   ```
   ChatMessage{Role: "alert_" + string(p.Level), Blocks: []ContentBlock{BlockAlert{Level: string(p.Level), Message: p.Message}}}
   ```

d) **TypeAgentTransition** (line ~703): Update recordHandoff to create BlockHandoff in the message.

**Step 4: Update streaming finalization** (TypeDone handler):
- At TypeDone, call flushStreamBuffer() to commit remaining text as BlockText
- Then create the finalized ChatMessage using the accumulated Blocks

**Step 5: Update renderMessage in views.go** (line ~361):
- Change `msg.Content` access to `msg.Content()` for the backward-compatible path
- This is a minimal change -- Phase 3 will add per-block-type rendering

**Step 6: Add integration tests** to content_test.go:
- TestHandleProtocol_TableCreatesBlockTable: Verify TypeTable protocol message results in BlockTable in message Blocks
- TestHandleProtocol_CodeCreatesBlockCode: Verify TypeCode creates BlockCode
- TestStreamThenTable_BothBlocksPreserved: Simulate TypeText chunks then TypeTable, verify [BlockText, BlockTable] in Blocks

Ensure `go build ./...` compiles and `go test ./...` passes.
  </action>
  <verify>
    <automated>cd /Users/tyo/Omics-OS/lobster/lobster-tui && go build ./... && go test ./internal/chat/ -count=1 -v</automated>
  </verify>
  <done>
    - ChatMessage.Content string field replaced with Blocks []ContentBlock
    - All .Content field accesses migrated to .Content() method calls or Blocks assignments
    - Protocol handlers (TypeCode, TypeTable, TypeAlert, TypeAgentTransition) create typed blocks
    - flushStreamBuffer() called before every typed block append
    - Existing tests pass (no regressions)
    - New integration tests pass
    - `go build ./...` succeeds (full compilation check)
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/tyo/Omics-OS/lobster/lobster-tui && go build ./... && go test ./... -count=1
```
All packages compile. All tests pass (existing + new).
</verification>

<success_criteria>
- ChatMessage uses typed []ContentBlock instead of flat string (DATA-01)
- Protocol handlers create BlockTable/BlockCode/BlockAlert/BlockHandoff (DATA-02)
- Streaming text flushed as BlockText before typed blocks (DATA-03)
- Content() helper provides backward compat for existing rendering (DATA-04)
- Zero regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
