---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lobster-tui/internal/chat/model.go
  - lobster-tui/internal/chat/component_lifecycle_test.go
  - lobster-tui/internal/protocol/types.go
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-05, COMP-06, COMP-07, COMP-08]

must_haves:
  truths:
    - "threshold_slider and cell_type_selector instantiate their native components (not text_input fallback)"
    - "Pressing Esc on an active component sends a cancel action distinct from submitting empty data"
    - "Replacing an active component sends cancel to the old one first"
    - "SetData updates only reach the matching active component (stale MsgID updates are discarded)"
    - "ChangeEvent fires after component value changes with debounce"
    - "Bad component Init sends error response without crashing the TUI"
  artifacts:
    - path: "lobster-tui/internal/chat/component_lifecycle_test.go"
      provides: "Unit tests for all 8 component lifecycle requirements"
      contains: "TestComponentRegistry"
    - path: "lobster-tui/internal/chat/model.go"
      provides: "Fixed component lifecycle handling"
      contains: "changeEventTick"
    - path: "lobster-tui/internal/protocol/types.go"
      provides: "New TypeComponentSetData message type"
      contains: "TypeComponentSetData"
  key_links:
    - from: "lobster-tui/internal/chat/model.go imports"
      to: "lobster-tui/internal/biocomp/celltype and threshold"
      via: "blank imports for init() side-effect registration"
      pattern: "_ .*biocomp/(celltype|threshold)"
    - from: "lobster-tui/internal/chat/model.go sendComponentResponse"
      to: "protocol.ComponentResponsePayload"
      via: "action field included in Data map"
      pattern: "action.*result\\.Action"
    - from: "lobster-tui/internal/chat/model.go protocol handler"
      to: "lobster-tui/internal/protocol/types.go TypeComponentSetData"
      via: "SetData routing from protocol message to activeComponent"
      pattern: "TypeComponentSetData"
---

<objective>
Fix 8 BioComp component lifecycle bugs: missing registry imports for threshold_slider/cell_type_selector, lost cancel action in sendComponentResponse, ChangeEvent never polled, SetData never routed, no error boundary on View/HandleMsg panic, and no stale MsgID guard on SetData.

Purpose: Components must work correctly before Phase 4 puts them in the footer layout. Fixing these now prevents compounding bugs.
Output: Fixed model.go, new protocol message type for SetData, comprehensive lifecycle tests.
</objective>

<execution_context>
@/Users/tyo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tyo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lobster-tui/internal/biocomp/component.go:
```go
type ComponentResult struct {
    Action string         // "submit" | "cancel"
    Data   map[string]any
}

type BioComponent interface {
    Init(data json.RawMessage) error
    HandleMsg(msg tea.Msg) *ComponentResult
    View(width, height int) string
    SetData(data json.RawMessage) error
    Name() string
    Mode() string
    KeyBindings() []key.Binding
    ChangeEvent() map[string]any  // Returns pending change or nil
}
```

From lobster-tui/internal/biocomp/registry.go:
```go
func Register(name string, factory Factory)
func Get(name string) Factory  // Returns nil if unknown
func Names() []string
```

From lobster-tui/internal/chat/model.go (current imports -- missing celltype and threshold):
```go
import (
    "github.com/the-omics-os/lobster-tui/internal/biocomp"
    _ "github.com/the-omics-os/lobster-tui/internal/biocomp/bioselect"
    _ "github.com/the-omics-os/lobster-tui/internal/biocomp/confirm"
    _ "github.com/the-omics-os/lobster-tui/internal/biocomp/qcdash"
    _ "github.com/the-omics-os/lobster-tui/internal/biocomp/textinput"
    // MISSING: celltype and threshold
)
```

From lobster-tui/internal/chat/model.go (sendComponentResponse -- drops action):
```go
func (m *Model) sendComponentResponse(msgID, action string, data map[string]any) {
    if m.handler == nil { return }
    _ = m.handler.SendTyped(protocol.TypeComponentResponse, protocol.ComponentResponsePayload{
        ID:   msgID,
        Data: data,   // action parameter is DROPPED here
    }, msgID)
}
```

From lobster-tui/internal/chat/model.go (handleComponentRender -- already handles replacement):
```go
func (m Model) handleComponentRender(p protocol.ComponentRenderPayload, msgID string) (tea.Model, tea.Cmd) {
    if m.activeComponent != nil {
        m.sendComponentResponse(m.activeComponent.MsgID, "cancel", map[string]any{"reason": "replaced"})
        m.activeComponent = nil
    }
    // ... factory lookup, Init, etc.
}
```

From lobster-tui/internal/protocol/types.go:
```go
const TypeComponentResponse = "component_response"
type ComponentResponsePayload struct {
    ID   string         `json:"id"`
    Data map[string]any `json:"data,omitempty"`
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write component lifecycle tests + fix imports and cancel action</name>
  <files>lobster-tui/internal/chat/component_lifecycle_test.go, lobster-tui/internal/chat/model.go</files>
  <behavior>
    - TestComponentRegistry_AllRegistered: biocomp.Names() includes "threshold_slider" and "cell_type_selector" (after adding blank imports)
    - TestCancelAction_IncludedInResponse: When sendComponentResponse is called with action="cancel", the Data map sent to protocol includes key "action" with value "cancel"
    - TestComponentReplacement_CancelsOld: When handleComponentRender is called with an active component, it sends cancel response for the old component before initializing new one (verify via mock handler)
    - TestOverlaySize_SharedBetweenLayoutAndView: layoutReservedRows() and the overlay View() call use the same size computation (extract helper, test it returns consistent values)
    - TestErrorBoundary_BadInitSendsError: If comp.Init() returns error, sendComponentResponse is called with action="error" (already works per line 2412-2414, but add test to lock it in)
    - TestErrorBoundary_PanicInViewRecovers: If comp.View() panics, handleComponentRender recovers and sends error response
  </behavior>
  <action>
**Step 1: Create component_lifecycle_test.go** with test stubs for all COMP-* requirements. Tests use a mock BioComponent and mock protocol.Handler to verify behavior without needing a full TUI.

Create a `mockComponent` struct implementing `biocomp.BioComponent`:
- Configurable Init error, HandleMsg result, ChangeEvent return, SetData behavior
- Tracks calls for assertions

Create a `mockHandler` that captures SendTyped calls for verifying protocol messages sent.

**Step 2: Fix COMP-01 (missing imports)**
Add blank imports to model.go:
```go
_ "github.com/the-omics-os/lobster-tui/internal/biocomp/celltype"
_ "github.com/the-omics-os/lobster-tui/internal/biocomp/threshold"
```
This ensures init() in those packages registers their factories with the biocomp.Registry.

**Step 3: Fix COMP-02 (cancel action dropped)**
In `sendComponentResponse`, include the action in the Data map:
```go
func (m *Model) sendComponentResponse(msgID, action string, data map[string]any) {
    if m.handler == nil { return }
    if data == nil { data = map[string]any{} }
    data["action"] = action  // ADD THIS LINE
    _ = m.handler.SendTyped(protocol.TypeComponentResponse, protocol.ComponentResponsePayload{
        ID:   msgID,
        Data: data,
    }, msgID)
}
```

**Step 4: Fix COMP-04 (layout size hints)**
Extract the overlay size selection logic (currently duplicated between layoutReservedRows and the overlay View section) into a method on ActiveComponent or a standalone function:
```go
func overlayHeight(comp biocomp.BioComponent, termHeight int) int
```
Call this from both layoutReservedRows() and the overlay rendering in View(). The function should use the same clamping logic currently in both sites.

**Step 5: Fix COMP-07 (error boundary)**
The Init error case is already handled (line 2412-2414). Add recover() protection around comp.View() and comp.HandleMsg() calls. In the key handling section where HandleMsg is called (~line 1001-1009), wrap in a deferred recover:
```go
func safeHandleMsg(comp biocomp.BioComponent, msg tea.Msg) (result *biocomp.ComponentResult, panicked bool) {
    defer func() {
        if r := recover(); r != nil {
            panicked = true
        }
    }()
    return comp.HandleMsg(msg), false
}
```
If panicked, send error response and clear activeComponent.

Write tests RED first, then implement GREEN.
  </action>
  <verify>
    <automated>cd /Users/tyo/Omics-OS/lobster/lobster-tui && go test ./internal/chat/ -run "TestComponentRegistry|TestCancelAction|TestComponentReplacement|TestOverlaySize|TestErrorBoundary" -count=1 -v</automated>
  </verify>
  <done>
    - threshold_slider and cell_type_selector appear in biocomp.Names() (COMP-01)
    - sendComponentResponse includes action in Data map (COMP-02)
    - Component replacement verified by test (COMP-03)
    - Overlay size extracted to shared function (COMP-04)
    - Error boundary covers Init, View, HandleMsg panics (COMP-07)
    - All lifecycle tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ChangeEvent debounce, SetData routing, and stale guard</name>
  <files>lobster-tui/internal/chat/model.go, lobster-tui/internal/protocol/types.go, lobster-tui/internal/chat/component_lifecycle_test.go</files>
  <behavior>
    - TestChangeEvent_DebouncedAt50ms: After HandleMsg returns nil but ChangeEvent() returns data, a changeEventTick is scheduled; when tick fires, change_event protocol message is sent with the component data
    - TestChangeEvent_MultipleWithinDebounce: Two rapid ChangeEvent values within 50ms -- only the last one is sent when tick fires
    - TestSetData_RoutesToActiveComponent: When TypeComponentSetData protocol message arrives with matching MsgID, activeComponent.SetData() is called with the payload data
    - TestStaleSetData_DiscardedOnMismatch: When TypeComponentSetData arrives with a MsgID that doesn't match activeComponent.MsgID, SetData is NOT called and no error is sent
    - TestSetData_NoActiveComponent: When TypeComponentSetData arrives with no active component, message is silently discarded
  </behavior>
  <action>
**Step 1: Add TypeComponentSetData to protocol** (types.go):
```go
// TypeComponentSetData pushes updated data to an active BioComp component.
TypeComponentSetData = "component_set_data"
```
Add payload struct:
```go
type ComponentSetDataPayload struct {
    ID   string          `json:"id"`
    Data json.RawMessage `json:"data"`
}
```

**Step 2: Add ChangeEvent debounce fields** to Model struct in model.go:
```go
pendingChangeEvent  map[string]any
changeEventMsgID    string
changeDebounceActive bool
```
Add internal message type:
```go
type changeEventTick struct{}
```

**Step 3: Poll ChangeEvent after HandleMsg** in the component key handling section (~line 1001-1009 in model.go):
After `result := safeHandleMsg(m.activeComponent.Component, msg)` (using the safe wrapper from Task 1), if result is nil (user still interacting), check ChangeEvent:
```go
if ce := m.activeComponent.Component.ChangeEvent(); ce != nil {
    m.pendingChangeEvent = ce
    m.changeEventMsgID = m.activeComponent.MsgID
    if !m.changeDebounceActive {
        m.changeDebounceActive = true
        return m, tea.Tick(50*time.Millisecond, func(time.Time) tea.Msg {
            return changeEventTick{}
        })
    }
}
```

**Step 4: Handle changeEventTick** in the main Update switch:
```go
case changeEventTick:
    m.changeDebounceActive = false
    if m.pendingChangeEvent != nil && m.activeComponent != nil {
        // Send change event via protocol
        if m.handler != nil {
            _ = m.handler.SendTyped("change_event", map[string]any{
                "id":   m.changeEventMsgID,
                "data": m.pendingChangeEvent,
            }, m.changeEventMsgID)
        }
        m.pendingChangeEvent = nil
    }
    return m, nil
```

**Step 5: Handle TypeComponentSetData** in the protocol message handler switch:
```go
case protocol.TypeComponentSetData:
    var p protocol.ComponentSetDataPayload
    if err := protocol.DecodePayload(msg.Message, &p); err == nil {
        // COMP-08: Stale guard -- only route if MsgID matches
        if m.activeComponent != nil && m.activeComponent.MsgID == p.ID {
            _ = m.activeComponent.Component.SetData(p.Data)
        }
        // Silently discard if no active component or MsgID mismatch
    }
    return m, waitForProtocolMsg(m.handler)
```

**Step 6: Add tests** for ChangeEvent and SetData behavior to component_lifecycle_test.go.

Ensure `go build ./...` compiles and all tests pass.
  </action>
  <verify>
    <automated>cd /Users/tyo/Omics-OS/lobster/lobster-tui && go build ./... && go test ./internal/chat/ -run "TestChangeEvent|TestSetData|TestStaleSetData" -count=1 -v</automated>
  </verify>
  <done>
    - ChangeEvent polled after HandleMsg with 50ms debounce (COMP-05)
    - SetData routed to active component via new TypeComponentSetData protocol message (COMP-06)
    - Error boundary covers View/HandleMsg panics (COMP-07 -- from Task 1)
    - Stale MsgID guard discards mismatched SetData (COMP-08)
    - New protocol message type TypeComponentSetData added
    - All lifecycle tests pass
    - `go build ./...` succeeds
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/tyo/Omics-OS/lobster/lobster-tui && go build ./... && go test ./... -count=1
```
All packages compile. All tests pass (existing + new lifecycle tests).
</verification>

<success_criteria>
- threshold_slider and cell_type_selector instantiate native components (COMP-01)
- Esc sends cancel action distinct from empty submission (COMP-02)
- Active component replacement cancels old first (COMP-03)
- Layout size hints shared between layoutReservedRows and View (COMP-04)
- ChangeEvent fires with 50ms debounce (COMP-05)
- SetData routes to active component via protocol (COMP-06)
- Error boundary prevents crash on bad Init/View/HandleMsg (COMP-07)
- Stale MsgID guard discards wrong-component SetData (COMP-08)
- Zero regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
