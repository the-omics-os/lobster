---
phase: 01-geo-safety-contract-hotfixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lobster/core/data_manager_v2.py
  - lobster/services/data_access/geo_service.py
  - lobster/services/data_access/geo_queue_preparer.py
  - lobster/tools/workspace_tool.py
  - tests/unit/core/test_metadata_key_consistency.py
  - tests/unit/core/test_store_geo_metadata.py
autonomous: true
requirements: [GSAF-02, GSAF-03]

must_haves:
  truths:
    - "MetadataEntry TypedDict uses 'validation_result' key (not 'validation')"
    - "All callers of _store_geo_metadata pass validation_result= kwarg (not validation=)"
    - "No direct metadata_store[x] = assignments exist in geo_service.py -- all writes go through _store_geo_metadata or _enrich_geo_metadata"
    - "geo_queue_preparer cached.get('validation_result') reads correctly from MetadataEntry"
  artifacts:
    - path: "lobster/core/data_manager_v2.py"
      provides: "MetadataEntry with validation_result key, _store_geo_metadata and _enrich_geo_metadata helpers"
      contains: "validation_result"
    - path: "tests/unit/core/test_metadata_key_consistency.py"
      provides: "Tests for metadata key standardization"
      min_lines: 40
    - path: "tests/unit/core/test_store_geo_metadata.py"
      provides: "Tests for centralized write enforcement"
      min_lines: 40
  key_links:
    - from: "lobster/services/data_access/geo_service.py"
      to: "lobster/core/data_manager_v2.py"
      via: "_store_geo_metadata and _enrich_geo_metadata calls"
      pattern: "_store_geo_metadata\\(|_enrich_geo_metadata\\("
    - from: "lobster/services/data_access/geo_queue_preparer.py"
      to: "lobster/core/data_manager_v2.py"
      via: "cached.get('validation_result') reading MetadataEntry"
      pattern: "validation_result"
---

<objective>
Standardize the GEO metadata validation key and enforce centralized metadata writes.

Purpose: The MetadataEntry TypedDict uses "validation" but DownloadQueueEntry and all callers use "validation_result", causing silent None returns when reading cached metadata. Additionally, 4 direct metadata_store assignments in geo_service.py bypass the _store_geo_metadata helper, creating malformed entries. This plan fixes the contract mismatch and eliminates all bypass sites.

Output: Consistent metadata key across all GEO components, zero direct metadata_store writes in geo_service.py, tests proving both invariants hold.
</objective>

<execution_context>
@/Users/tyo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tyo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-geo-safety-contract-hotfixes/01-CONTEXT.md
@.planning/phases/01-geo-safety-contract-hotfixes/01-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lobster/core/data_manager_v2.py (line 162):
```python
class MetadataEntry(TypedDict, total=False):
    metadata: Dict[str, Any]
    validation: Dict[str, Any]        # BUG: should be "validation_result"
    fetch_timestamp: str
    strategy_config: Dict[str, Any]
    stored_by: str
    modality_detection: Dict[str, Any]
    concatenation_decision: Dict[str, Any]
```

From lobster/core/data_manager_v2.py (line 2534):
```python
def _store_geo_metadata(self, geo_id: str, metadata: Dict[str, Any], stored_by: str, **kwargs) -> MetadataEntry:
    # kwargs uses "validation" key (line 2574): if "validation" in kwargs: entry["validation"] = kwargs["validation"]
```

From lobster/core/schemas/download_queue.py (line 205):
```python
class DownloadQueueEntry(BaseModel):
    # Uses "validation_result" field -- this is the correct public API
```

Direct metadata_store assignments to eliminate (geo_service.py):
- Line 2534: self.data_manager.metadata_store[geo_id] = existing_entry  (in _validate_and_enrich_metadata)
- Line 2586: self.data_manager.metadata_store[geo_id] = existing_entry  (in _validate_and_enrich_metadata, multimodal)
- Line 5695: self.data_manager.metadata_store[modality_name] = existing_entry  (in _handle_multi_sample_concatenation)
- Line 5699: self.data_manager.metadata_store[modality_name] = {...}  (in _handle_multi_sample_concatenation, fallback)

Callers of _store_geo_metadata that pass validation= kwarg:
- geo_service.py line 534, 845, 2276, 2352, 2543
- geo_queue_preparer.py line 342
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Standardize MetadataEntry key and _store_geo_metadata contract (GSAF-02)</name>
  <files>
    lobster/core/data_manager_v2.py,
    lobster/services/data_access/geo_queue_preparer.py,
    lobster/tools/workspace_tool.py,
    tests/unit/core/test_metadata_key_consistency.py
  </files>
  <behavior>
    - Test: MetadataEntry TypedDict has "validation_result" key, not "validation"
    - Test: _store_geo_metadata accepts validation_result= kwarg and stores it under "validation_result"
    - Test: _store_geo_metadata raises ValueError if metadata dict is missing/None
    - Test: _store_geo_metadata warns (logger.warning) if validation_result is None
    - Test: _get_geo_metadata returns entry with "validation_result" key accessible
  </behavior>
  <action>
    1. Create `tests/unit/core/test_metadata_key_consistency.py` with tests for the above behaviors. Tests should instantiate DataManagerV2 (or mock it minimally) and verify the key contract.

    2. In `lobster/core/data_manager_v2.py`:
       a. Rename `MetadataEntry.validation` to `validation_result` (line 180)
       b. Update the docstring (lines 163-177) to reference "validation_result"
       c. In `_store_geo_metadata` (line 2574): change `if "validation" in kwargs` to `if "validation_result" in kwargs` and `entry["validation"] = kwargs["validation"]` to `entry["validation_result"] = kwargs["validation_result"]`
       d. Add validation at method start: `if metadata is None: raise ValueError("metadata dict is required for _store_geo_metadata")`
       e. Add warning: `if kwargs.get("validation_result") is None and "validation_result" in kwargs: logger.warning(f"validation_result is None for {geo_id}")`

    3. Update ALL callers of `_store_geo_metadata(..., validation=...)` to use `validation_result=`:
       - `geo_service.py` lines 534, 845, 2276, 2352, 2543 -- rename kwarg from `validation=` to `validation_result=`
       - `geo_queue_preparer.py` line 342 -- rename kwarg from `validation=` to `validation_result=`

    4. Verify `workspace_tool.py` already uses "validation_result" (lines 1089-1091, 1299) -- no changes needed, but confirm reads align with new MetadataEntry key.

    5. Run tests to verify key consistency.
  </action>
  <verify>
    <automated>cd /Users/tyo/Omics-OS/lobster && python -m pytest tests/unit/core/test_metadata_key_consistency.py -x --no-cov -q --timeout=60</automated>
  </verify>
  <done>
    - MetadataEntry TypedDict uses "validation_result" (not "validation")
    - _store_geo_metadata accepts and stores "validation_result" kwarg
    - All 6 callers pass validation_result= (not validation=)
    - workspace_tool reads "validation_result" correctly from MetadataEntry
    - ValueError raised on missing metadata, warning on None validation_result
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce centralized metadata writes via _store_geo_metadata (GSAF-03)</name>
  <files>
    lobster/core/data_manager_v2.py,
    lobster/services/data_access/geo_service.py,
    tests/unit/core/test_store_geo_metadata.py
  </files>
  <behavior>
    - Test: _enrich_geo_metadata updates existing entry fields without recreating the entry
    - Test: _enrich_geo_metadata returns None and logs warning if entry does not exist
    - Test: _enrich_geo_metadata preserves existing fields while adding new ones
    - Test: After enrichment, metadata_store contains correct merged entry
  </behavior>
  <action>
    1. Create `tests/unit/core/test_store_geo_metadata.py` with tests for the enrichment pattern described above.

    2. In `lobster/core/data_manager_v2.py`, add `_enrich_geo_metadata` method (after `_store_geo_metadata`, around line 2595):
       ```python
       def _enrich_geo_metadata(self, geo_id: str, **fields) -> Optional[MetadataEntry]:
           """Add or update fields on existing metadata entry. Returns updated entry or None."""
           entry = self._get_geo_metadata(geo_id)
           if entry is None:
               logger.warning(f"Cannot enrich metadata for {geo_id}: no existing entry")
               return None
           entry.update(fields)
           self.metadata_store[geo_id] = entry  # Single controlled re-assignment point
           logger.debug(f"Enriched GEO metadata for {geo_id} with fields: {list(fields.keys())}")
           return entry
       ```

    3. In `lobster/services/data_access/geo_service.py`, replace ALL 4 direct metadata_store assignments with helper calls:

       a. Lines ~2534 (in _validate_and_enrich_metadata): Replace `self.data_manager.metadata_store[geo_id] = existing_entry` with `self.data_manager._enrich_geo_metadata(geo_id, **{k: v for k, v in existing_entry.items() if k not in ("metadata", "fetch_timestamp", "stored_by")})` -- or more precisely, identify what fields are being added/updated in `existing_entry` at that point (likely validation_result) and call `_enrich_geo_metadata(geo_id, validation_result=validation_data)`.

       b. Lines ~2586 (in _validate_and_enrich_metadata, multimodal path): Same pattern -- identify the enrichment fields and call `_enrich_geo_metadata`.

       c. Lines ~5695 (in _handle_multi_sample_concatenation): Replace `self.data_manager.metadata_store[modality_name] = existing_entry` with `_enrich_geo_metadata(modality_name, concatenation_decision=...)` using the specific fields being added.

       d. Lines ~5699 (in _handle_multi_sample_concatenation, fallback): This creates a new entry -- replace with `_store_geo_metadata(modality_name, metadata={...}, stored_by="_handle_multi_sample_concatenation", concatenation_decision=...)`.

       IMPORTANT: Read each site carefully to understand what fields are being set. The goal is zero `self.data_manager.metadata_store[` assignments remaining in geo_service.py.

    4. Verify no direct assignments remain: `grep -n "metadata_store\[" lobster/services/data_access/geo_service.py` should show only read access (`.get()`, `[x]["key"]`, `in metadata_store`) and zero `= {` or `= existing_entry` patterns.
  </action>
  <verify>
    <automated>cd /Users/tyo/Omics-OS/lobster && python -m pytest tests/unit/core/test_store_geo_metadata.py tests/unit/core/test_metadata_key_consistency.py -x --no-cov -q --timeout=60</automated>
  </verify>
  <done>
    - _enrich_geo_metadata method exists on DataManagerV2 and handles update + missing entry cases
    - All 4 direct metadata_store write sites in geo_service.py are replaced with helper calls
    - `grep -n "metadata_store\[.*\] =" lobster/services/data_access/geo_service.py` returns zero matches
    - Both test files pass
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/unit/core/test_metadata_key_consistency.py tests/unit/core/test_store_geo_metadata.py -x --no-cov -q` -- both test files pass
2. `grep -rn '"validation"' lobster/core/data_manager_v2.py` -- no remaining old key name in MetadataEntry or _store_geo_metadata (may appear in unrelated code/comments)
3. `grep -n 'metadata_store\[.*\] =' lobster/services/data_access/geo_service.py` -- zero direct write assignments
4. `grep -rn 'validation=' lobster/services/data_access/geo_service.py lobster/services/data_access/geo_queue_preparer.py | grep '_store_geo_metadata'` -- zero old kwarg usage (should be validation_result=)
</verification>

<success_criteria>
- MetadataEntry TypedDict and _store_geo_metadata both use "validation_result" key consistently
- All GEO metadata writes in geo_service.py go through _store_geo_metadata or _enrich_geo_metadata
- workspace_tool and geo_queue_preparer read "validation_result" from MetadataEntry successfully
- All new tests pass, no regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-geo-safety-contract-hotfixes/01-01-SUMMARY.md`
</output>
