---
phase: 02-charm-v2-migration
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lobster-tui/go.mod
  - lobster-tui/internal/chat/model.go
  - lobster-tui/internal/chat/views.go
  - lobster-tui/internal/chat/run.go
  - lobster-tui/internal/chat/completions.go
  - lobster-tui/internal/chat/content.go
  - lobster-tui/internal/chat/forms.go
  - lobster-tui/internal/chat/model_test.go
  - lobster-tui/internal/chat/content_test.go
  - lobster-tui/internal/chat/completions_test.go
  - lobster-tui/internal/chat/run_test.go
  - lobster-tui/internal/chat/views_intro_test.go
  - lobster-tui/internal/chat/component_lifecycle_test.go
  - lobster-tui/internal/theme/theme.go
  - lobster-tui/internal/theme/lobster.go
  - lobster-tui/internal/theme/loader.go
  - lobster-tui/internal/protocol/handler.go
  - lobster-tui/internal/protocol/types.go
  - lobster-tui/internal/biocomp/component.go
  - lobster-tui/internal/biocomp/overlay.go
  - lobster-tui/internal/biocomp/overlay_test.go
  - lobster-tui/internal/biocomp/registry.go
  - lobster-tui/internal/biocomp/registry_test.go
  - lobster-tui/internal/biocomp/confirm/confirm.go
  - lobster-tui/internal/biocomp/confirm/confirm_test.go
  - lobster-tui/internal/biocomp/bioselect/select.go
  - lobster-tui/internal/biocomp/bioselect/select_test.go
  - lobster-tui/internal/biocomp/textinput/textinput.go
  - lobster-tui/internal/biocomp/textinput/textinput_test.go
  - lobster-tui/internal/biocomp/threshold/threshold.go
  - lobster-tui/internal/biocomp/threshold/threshold_test.go
  - lobster-tui/internal/biocomp/celltype/celltype.go
  - lobster-tui/internal/biocomp/celltype/celltype_test.go
  - lobster-tui/internal/biocomp/qcdash/qcdash.go
  - lobster-tui/internal/biocomp/qcdash/qcdash_test.go
  - lobster-tui/internal/ui/views/views.go
  - lobster-tui/cmd/lobster-tui/main.go
autonomous: true
requirements: [MIGR-01, MIGR-02, MIGR-04]

must_haves:
  truths:
    - "All Go source files import charm.land/*/v2 instead of github.com/charmbracelet/{bubbletea,lipgloss,bubbles}"
    - "View() on the top-level chat Model returns tea.View (not string)"
    - "Colors struct fields use color.Color from image/color standard library"
    - "All lipgloss styles compile and render correctly with v2 types"
    - "glamour stays on github.com/charmbracelet/glamour (no v2 exists)"
  artifacts:
    - path: "lobster-tui/internal/theme/theme.go"
      provides: "Colors struct with color.Color fields"
      contains: "color.Color"
    - path: "lobster-tui/internal/chat/model.go"
      provides: "View() returning tea.View"
      contains: "tea.NewView"
    - path: "lobster-tui/internal/chat/run.go"
      provides: "v2 Program creation with View-field options"
      contains: "charm.land/bubbletea/v2"
  key_links:
    - from: "lobster-tui/go.mod"
      to: "charm.land/bubbletea/v2"
      via: "require directive"
      pattern: "charm\\.land/bubbletea/v2"
    - from: "lobster-tui/internal/theme/theme.go"
      to: "image/color"
      via: "import"
      pattern: "image/color"
---

<objective>
Mechanically migrate all imports from Charm v1 to v2, update View() return type, and migrate color types.

Purpose: This is the bulk mechanical migration. Every .go file switches from github.com/charmbracelet/* to charm.land/*/v2. The Colors struct changes from lipgloss.Color to color.Color. The top-level View() wraps its string in tea.NewView(). Program options (WithAltScreen, WithMouseCellMotion) move to View fields. After this plan, the binary compiles on v2 with the exception of KeyMsg handling (still using v1 patterns -- Plan 03 handles that) and huh (still imported in wizard/forms -- Plan 03 removes it).

Output: All 28 files migrated to v2 imports, compiling binary, passing tests (except wizard/forms tests that depend on huh -- those are addressed in Plan 03).
</objective>

<execution_context>
@/Users/tyo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tyo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-charm-v2-migration/02-RESEARCH.md
@.planning/phases/02-charm-v2-migration/02-01-SUMMARY.md

<interfaces>
<!-- From 02-RESEARCH.md - Import path mapping -->
v1: github.com/charmbracelet/bubbletea -> v2: charm.land/bubbletea/v2
v1: github.com/charmbracelet/lipgloss -> v2: charm.land/lipgloss/v2
v1: github.com/charmbracelet/bubbles/* -> v2: charm.land/bubbles/v2/*
v1: github.com/charmbracelet/huh -> REMOVED (Plan 03)
v1: github.com/charmbracelet/glamour -> NO CHANGE (no v2)

<!-- From theme.go - Colors struct that changes -->
type Colors struct {
    Primary    lipgloss.Color  // -> color.Color
    Secondary  lipgloss.Color  // -> color.Color
    // ... 15 fields total
}

<!-- From run.go - Program options that move to View -->
tea.WithAltScreen()        -> view.AltScreen = true
tea.WithMouseCellMotion()  -> view.MouseMode = tea.MouseModeCellMotion

<!-- From model.go - View signature that changes -->
func (m Model) View() string -> func (m Model) View() tea.View
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate imports, Colors struct, and color values across all files</name>
  <files>
    lobster-tui/internal/theme/theme.go,
    lobster-tui/internal/theme/lobster.go,
    lobster-tui/internal/theme/loader.go,
    lobster-tui/internal/chat/model.go,
    lobster-tui/internal/chat/views.go,
    lobster-tui/internal/chat/completions.go,
    lobster-tui/internal/chat/content.go,
    lobster-tui/internal/chat/run.go,
    lobster-tui/internal/chat/forms.go,
    lobster-tui/internal/protocol/handler.go,
    lobster-tui/internal/protocol/types.go,
    lobster-tui/internal/biocomp/component.go,
    lobster-tui/internal/biocomp/overlay.go,
    lobster-tui/internal/biocomp/registry.go,
    lobster-tui/internal/biocomp/confirm/confirm.go,
    lobster-tui/internal/biocomp/bioselect/select.go,
    lobster-tui/internal/biocomp/textinput/textinput.go,
    lobster-tui/internal/biocomp/threshold/threshold.go,
    lobster-tui/internal/biocomp/celltype/celltype.go,
    lobster-tui/internal/biocomp/qcdash/qcdash.go,
    lobster-tui/internal/ui/views/views.go,
    lobster-tui/cmd/lobster-tui/main.go,
    lobster-tui/internal/chat/model_test.go,
    lobster-tui/internal/chat/content_test.go,
    lobster-tui/internal/chat/completions_test.go,
    lobster-tui/internal/chat/run_test.go,
    lobster-tui/internal/chat/views_intro_test.go,
    lobster-tui/internal/chat/component_lifecycle_test.go,
    lobster-tui/internal/biocomp/overlay_test.go,
    lobster-tui/internal/biocomp/registry_test.go,
    lobster-tui/internal/biocomp/confirm/confirm_test.go,
    lobster-tui/internal/biocomp/bioselect/select_test.go,
    lobster-tui/internal/biocomp/textinput/textinput_test.go,
    lobster-tui/internal/biocomp/threshold/threshold_test.go,
    lobster-tui/internal/biocomp/celltype/celltype_test.go,
    lobster-tui/internal/biocomp/qcdash/qcdash_test.go
  </files>
  <action>
Perform three mechanical transformations across ALL .go files (source + test):

**A. Import path migration (all 28+ files that import v1 Charm packages):**
Replace import paths in every file:
- `github.com/charmbracelet/bubbletea` -> `charm.land/bubbletea/v2`
- `github.com/charmbracelet/lipgloss` -> `charm.land/lipgloss/v2`
- `github.com/charmbracelet/bubbles/viewport` -> `charm.land/bubbles/v2/viewport`
- `github.com/charmbracelet/bubbles/textarea` -> `charm.land/bubbles/v2/textarea`
- `github.com/charmbracelet/bubbles/textinput` -> `charm.land/bubbles/v2/textinput`
- `github.com/charmbracelet/bubbles/key` -> `charm.land/bubbles/v2/key`

DO NOT change: `github.com/charmbracelet/glamour` (no v2 exists).
DO NOT change: `github.com/charmbracelet/huh` imports in wizard.go and forms.go yet (Plan 03 handles huh removal).

Keep the same import alias pattern: `tea "charm.land/bubbletea/v2"` etc.

**B. Colors struct type migration (theme.go):**
In `internal/theme/theme.go`:
- Add `"image/color"` import
- Change all 15 Colors struct fields from `lipgloss.Color` to `color.Color`

The initialization syntax in lobster.go stays the same: `lipgloss.Color("#e45c47")` -- in v2 this is a function call returning color.Color instead of a type conversion. So lobster.go initializations just work.

In loader.go, if there are any explicit `lipgloss.Color` type references for color parsing, update them to return `color.Color`.

**C. View() return type + Program options (model.go, run.go):**
In `internal/chat/model.go`:
- Change `func (m Model) View() string` to `func (m Model) View() tea.View`
- At the end of View(), wrap the return: `return tea.NewView(s)` instead of `return s`

In `internal/chat/run.go`:
- Remove `tea.WithAltScreen()` and `tea.WithMouseCellMotion()` from Program options
- The Model's View() must set these: add fields to the tea.View before returning:
  ```go
  v := tea.NewView(s)
  if !m.inline {
      v.AltScreen = true
  }
  if m.mouseCapture {
      v.MouseMode = tea.MouseModeCellMotion
  }
  return v
  ```
  (mouseCapture is already stored on Model from NewModel -- verify field exists, add if not)

After all changes, run `cd lobster-tui && go mod tidy` to update go.mod/go.sum.

**IMPORTANT:** This task intentionally does NOT change tea.KeyMsg to tea.KeyPressMsg. The key handling migration is in Plan 03. The code will NOT compile after this task alone because tea.KeyMsg does not exist in v2. This is expected -- Task 1 is committed as a WIP, and Plan 03 immediately follows. HOWEVER, if the code CAN compile by temporarily keeping v1 bubbletea alongside v2 in go.mod, prefer that approach so we maintain a compiling state. Check: if `tea.KeyMsg` still exists in bubbletea v2 as a deprecated alias, we can defer key migration cleanly. If not, we need a different approach.

**Fallback approach if tea.KeyMsg does not exist in v2:** Also replace all `tea.KeyMsg` with `tea.KeyPressMsg` in this same task (the mechanical type switch), updating field access patterns:
- `msg.Type` checks -> `msg.String()` switch or `msg.Code` checks
- `tea.KeyMsg{Type: X, Runes: Y}` in tests -> `tea.KeyPressMsg{Code: X, Text: Y}`
- `msg.Alt` -> `msg.Mod.Contains(tea.ModAlt)`
- See the Key Constant Migration Table in 02-RESEARCH.md

Use the research document's Pattern 3 and Pattern 4 as the reference for every transformation. There are ~25 source sites and ~82 test sites for KeyMsg.

Run `go build ./...` to verify compilation (excluding initwizard and forms which still import huh).
  </action>
  <verify>
    <automated>cd /Users/tyo/Omics-OS/lobster/lobster-tui && go build ./cmd/... ./internal/chat/ ./internal/protocol/ ./internal/theme/ ./internal/biocomp/... ./internal/ui/... ./internal/spike/</automated>
  </verify>
  <done>All imports use charm.land/*/v2 paths (except glamour and huh). Colors struct uses color.Color. View() returns tea.View with AltScreen/MouseMode fields. Code compiles (excluding initwizard package which still imports huh).</done>
</task>

<task type="auto">
  <name>Task 2: Verify tests pass and validate no v1 imports remain in migrated files</name>
  <files>lobster-tui/go.mod</files>
  <action>
Run the full test suite for all migrated packages (excluding initwizard which still depends on huh):

```bash
cd lobster-tui && go test ./internal/chat/ ./internal/protocol/ ./internal/theme/ ./internal/biocomp/... ./internal/ui/... ./internal/spike/ -v -count=1
```

Fix any test failures caused by the migration (type mismatches, changed API signatures, etc.).

Then run the v1 import grep check on all files EXCEPT wizard.go and forms.go:

```bash
grep -r 'github.com/charmbracelet/\(bubbletea\|lipgloss\|bubbles\)' lobster-tui/ --include='*.go' | grep -v 'initwizard/' | grep -v 'forms.go' | grep -v 'glamour'
```

This MUST return empty. Any remaining v1 imports (other than glamour, initwizard, forms) indicate missed files.

Finally, run `go mod tidy` and verify go.mod is clean.
  </action>
  <verify>
    <automated>cd /Users/tyo/Omics-OS/lobster/lobster-tui && go test ./internal/chat/ ./internal/protocol/ ./internal/theme/ ./internal/biocomp/... ./internal/ui/... ./internal/spike/ -count=1</automated>
  </verify>
  <done>All tests pass for migrated packages. No v1 bubbletea/lipgloss/bubbles imports remain outside of initwizard/forms (which are migrated in Plan 03). grep check returns empty.</done>
</task>

</tasks>

<verification>
- `cd lobster-tui && go test ./internal/chat/ ./internal/protocol/ ./internal/theme/ ./internal/biocomp/... ./internal/ui/... ./internal/spike/ -count=1` -- all pass
- `grep -r 'github.com/charmbracelet/\(bubbletea\|lipgloss\|bubbles\)' lobster-tui/ --include='*.go' | grep -v initwizard | grep -v forms.go | grep -v glamour` -- returns empty
- `cd lobster-tui && go build ./cmd/... ./internal/chat/ ./internal/protocol/ ./internal/theme/ ./internal/biocomp/... ./internal/ui/...` -- compiles
</verification>

<success_criteria>
All 28+ files use v2 import paths. Colors struct fields are color.Color. View() returns tea.View. Program options moved to View fields. All tests pass for migrated packages. Only initwizard/ and forms.go retain v1/huh imports (addressed in Plan 03).
</success_criteria>

<output>
After completion, create `.planning/phases/02-charm-v2-migration/02-02-SUMMARY.md`
</output>
