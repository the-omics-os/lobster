---
phase: 04-agent-rollout
plan: 07
type: execute
wave: 3
depends_on: ["04-03", "04-04"]
files_modified:
  - packages/lobster-drug-discovery/lobster/agents/drug_discovery/drug_discovery_expert.py
  - packages/lobster-drug-discovery/lobster/agents/drug_discovery/shared_tools.py
  - packages/lobster-drug-discovery/lobster/agents/drug_discovery/cheminformatics_tools.py
  - packages/lobster-drug-discovery/lobster/agents/drug_discovery/clinical_tools.py
  - packages/lobster-drug-discovery/lobster/agents/drug_discovery/pharmacogenomics_tools.py
  - packages/lobster-drug-discovery/tests/__init__.py
  - packages/lobster-drug-discovery/tests/agents/__init__.py
  - packages/lobster-drug-discovery/tests/agents/test_aquadif_drug_discovery.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - ROLL-09
  - ROLL-10

must_haves:
  truths:
    - "All 10 shared_tools.py tools in drug-discovery have .metadata and .tags"
    - "All 9 cheminformatics tools have .metadata and .tags"
    - "All 8 clinical tools have .metadata and .tags"
    - "All 8 pharmacogenomics tools have .metadata and .tags"
    - "Contract tests pass for drug_discovery_expert (parent), cheminformatics_expert, clinical_dev_expert, and pharmacogenomics_expert"
    - "ROLL-10 requirement added to REQUIREMENTS.md"
    - "Global ROLL-09 validation: multi-category usage is under 40% across all ~222 tagged tools"
    - "All existing tests in drug-discovery package still pass (if any exist)"
  artifacts:
    - path: "packages/lobster-drug-discovery/tests/agents/test_aquadif_drug_discovery.py"
      provides: "Contract test classes for 4 drug-discovery agents"
      contains: "AgentContractTestMixin"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirements with ROLL-10"
      contains: "ROLL-10"
  key_links:
    - from: "packages/lobster-drug-discovery/tests/agents/test_aquadif_drug_discovery.py"
      to: "lobster/testing/contract_mixins.py"
      via: "inherit AgentContractTestMixin"
      pattern: "class Test.*Aquadif\\(AgentContractTestMixin\\)"
---

<objective>
Tag all 35 tools in lobster-drug-discovery (4 agents + shared_tools) with AQUADIF metadata, create contract tests, add ROLL-10 to REQUIREMENTS.md, and perform the final global ROLL-09 validation across all ~222 tools.

Purpose: Drug-discovery is the largest and newest package. It was added to Phase 4 scope per user decision in CONTEXT.md. After tagging this package, all ~222 tools across the entire Lobster AI system will have AQUADIF metadata, completing the rollout. The global validation (ROLL-09) confirms system-wide compliance.

Output: 35 tools tagged across 5 files, 4 contract test classes passing, ROLL-10 in REQUIREMENTS.md, global multi-category ratio under 40%.
</objective>

<execution_context>
@/Users/tyo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tyo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-agent-rollout/04-CONTEXT.md
@.planning/phases/04-agent-rollout/04-RESEARCH.md
@skills/lobster-dev/references/aquadif-migration.md

<interfaces>
From lobster/config/aquadif.py:
```python
PROVENANCE_REQUIRED = frozenset({IMPORT, QUALITY, FILTER, PREPROCESS, ANALYZE, ANNOTATE, SYNTHESIZE})
```

Entry points:
- `drug_discovery_expert = "lobster.agents.drug_discovery.drug_discovery_expert:AGENT_CONFIG"` — parent
- `cheminformatics_expert = "lobster.agents.drug_discovery.cheminformatics_expert:AGENT_CONFIG"` — child
- `clinical_dev_expert = "lobster.agents.drug_discovery.clinical_dev_expert:AGENT_CONFIG"` — child
- `pharmacogenomics_expert = "lobster.agents.drug_discovery.pharmacogenomics_expert:AGENT_CONFIG"` — child

NOTE: This package may not have a `tests/` directory at all — create it from scratch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AQUADIF metadata to all 5 drug-discovery tool files and update REQUIREMENTS.md</name>
  <files>
    packages/lobster-drug-discovery/lobster/agents/drug_discovery/drug_discovery_expert.py,
    packages/lobster-drug-discovery/lobster/agents/drug_discovery/shared_tools.py,
    packages/lobster-drug-discovery/lobster/agents/drug_discovery/cheminformatics_tools.py,
    packages/lobster-drug-discovery/lobster/agents/drug_discovery/clinical_tools.py,
    packages/lobster-drug-discovery/lobster/agents/drug_discovery/pharmacogenomics_tools.py,
    .planning/REQUIREMENTS.md
  </files>
  <action>
Follow the migration guide at `skills/lobster-dev/references/aquadif-migration.md`. Use post-decorator 2-line inline pattern. String literals for categories.

**Step 1: Verify file structure**

Read the package directory to confirm actual file names and paths:
```bash
find packages/lobster-drug-discovery -name "*.py" | grep -v __pycache__
```

The research doc shows tool file names as `shared_tools.py`, `cheminformatics_tools.py`, `clinical_tools.py`, `pharmacogenomics_tools.py`. Verify these from the filesystem. If names differ, adjust accordingly.

**Step 2: Tag shared_tools.py (10 tools)**

| Tool Pattern | Category | Provenance | Rationale |
|-------------|----------|-----------|-----------|
| search_drug_targets | ["UTILITY"] | False | External search, no data modification |
| search_compounds | ["UTILITY"] | False | External search |
| score_drug_target | ["ANALYZE"] | True | Target scoring analysis |
| rank_targets | ["ANALYZE"] | True | Target ranking |
| get_compound_bioactivity | ["ANALYZE"] | True | Bioactivity retrieval + analysis |
| get_compound_properties | ["ANALYZE"] | True | Property computation |
| check_drug_discovery_status | ["UTILITY"] | False | Status check |
| list_available_databases | ["UTILITY"] | False | Listing |
| (others — apply 80% rule) | | | |

**Step 3: Tag cheminformatics_tools.py (9 tools)**

| Tool Pattern | Category | Provenance | Rationale |
|-------------|----------|-----------|-----------|
| calculate_descriptors | ["ANALYZE"] | True | Molecular descriptor computation |
| lipinski_check | ["QUALITY"] | True | Drug-likeness QC |
| fingerprint_similarity | ["ANALYZE"] | True | Molecular similarity |
| predict_admet | ["ANALYZE"] | True | ADMET prediction |
| (others — apply 80% rule) | | | |

**Step 4: Tag clinical_tools.py (8 tools)**

| Tool Pattern | Category | Provenance | Rationale |
|-------------|----------|-----------|-----------|
| get_drug_indications | ["ANNOTATE"] | True | Drug-indication annotation |
| (others — categorize based on function purpose) | | | |

**Step 5: Tag pharmacogenomics_tools.py (8 tools)**

| Tool Pattern | Category | Provenance | Rationale |
|-------------|----------|-----------|-----------|
| predict_mutation_effect | ["ANALYZE"] | True | Mutation impact prediction |
| extract_protein_embedding | ["ANALYZE"] | True | Embedding extraction |
| compare_variant_sequences | ["ANALYZE"] | True | Variant comparison |
| get_variant_drug_interactions | ["ANNOTATE"] | True | Drug-variant annotation |
| get_pharmacogenomic_evidence | ["ANNOTATE"] | True | Pharmacogenomics annotation |
| (others — apply 80% rule) | | | |

**Step 6: Tag drug_discovery_expert.py parent tools**

This parent agent may have its own dedicated tools beyond what's in shared_tools. Read the file and tag any tools found.

**Step 7: Update REQUIREMENTS.md**

Add ROLL-10 to the Agent Rollout section in `.planning/REQUIREMENTS.md`:

After ROLL-09, add:
```markdown
- [ ] **ROLL-10**: `drug_discovery_expert` (10 shared tools), `cheminformatics_expert` (9 tools), `clinical_dev_expert` (8 tools), and `pharmacogenomics_expert` (8 tools) have metadata and pass contract tests
```

Also add ROLL-10 to the Traceability table:
```markdown
| ROLL-10 | Phase 4 | Pending |
```

**CRITICAL provenance check:** Many drug-discovery tools are thin wrappers around external API calls. Verify each tool body for `log_tool_usage(..., ir=ir)` before assigning provenance. External search tools are UTILITY.
  </action>
  <verify>
    <automated>cd /Users/tyo/Omics-OS/lobster && python -c "
import re, glob
total_meta = 0
total_tools = 0
for path in glob.glob('packages/lobster-drug-discovery/lobster/agents/drug_discovery/*.py'):
    if '__' in path:
        continue
    with open(path) as f:
        src = f.read()
    meta = len(re.findall(r'\.metadata\s*=', src))
    tools = len(re.findall(r'@tool', src))
    total_meta += meta
    total_tools += tools
    name = path.split('/')[-1]
    print(f'{name}: {meta}/{tools}')
print(f'Total: {total_meta}/{total_tools} metadata assignments')
if total_meta >= total_tools:
    print('All drug-discovery tools tagged')
else:
    print('WARNING: some tools missing metadata')
"</automated>
  </verify>
  <done>
All 35 drug-discovery tools across 5 files have .metadata and .tags. ROLL-10 added to REQUIREMENTS.md. No existing code behavior modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create drug-discovery contract tests and run global ROLL-09 validation</name>
  <files>
    packages/lobster-drug-discovery/tests/__init__.py,
    packages/lobster-drug-discovery/tests/agents/__init__.py,
    packages/lobster-drug-discovery/tests/agents/test_aquadif_drug_discovery.py
  </files>
  <action>
**Step 1: Create test directory structure**

The drug-discovery package may have no `tests/` directory. Create:
```bash
mkdir -p packages/lobster-drug-discovery/tests/agents
touch packages/lobster-drug-discovery/tests/__init__.py
touch packages/lobster-drug-discovery/tests/agents/__init__.py
```

**Step 2: Create drug-discovery contract test**

Create `packages/lobster-drug-discovery/tests/agents/test_aquadif_drug_discovery.py`:

```python
"""
AQUADIF contract tests for the lobster-drug-discovery package.

Validates that all tools in drug_discovery_expert, cheminformatics_expert,
clinical_dev_expert, and pharmacogenomics_expert have correct AQUADIF metadata.

Run with: pytest -m contract tests/agents/test_aquadif_drug_discovery.py -v
"""
import pytest
from lobster.testing.contract_mixins import AgentContractTestMixin


@pytest.mark.contract
class TestAquadifDrugDiscoveryExpert(AgentContractTestMixin):
    """AQUADIF contract tests for drug_discovery_expert."""

    agent_module = "lobster.agents.drug_discovery.drug_discovery_expert"
    factory_name = "drug_discovery_expert"
    is_parent_agent = True  # Has children: cheminformatics, clinical_dev, pharmacogenomics


@pytest.mark.contract
class TestAquadifCheminformaticsExpert(AgentContractTestMixin):
    """AQUADIF contract tests for cheminformatics_expert."""

    agent_module = "lobster.agents.drug_discovery.cheminformatics_expert"
    factory_name = "cheminformatics_expert"
    is_parent_agent = False


@pytest.mark.contract
class TestAquadifClinicalDevExpert(AgentContractTestMixin):
    """AQUADIF contract tests for clinical_dev_expert."""

    agent_module = "lobster.agents.drug_discovery.clinical_dev_expert"
    factory_name = "clinical_dev_expert"
    is_parent_agent = False


@pytest.mark.contract
class TestAquadifPharmacogenomicsExpert(AgentContractTestMixin):
    """AQUADIF contract tests for pharmacogenomics_expert."""

    agent_module = "lobster.agents.drug_discovery.pharmacogenomics_expert"
    factory_name = "pharmacogenomics_expert"
    is_parent_agent = False
```

**Step 3: Run drug-discovery contract tests**

```bash
cd packages/lobster-drug-discovery && pytest -m contract tests/agents/test_aquadif_drug_discovery.py -v --timeout=120
```

**Step 4: Backward compatibility**

```bash
cd packages/lobster-drug-discovery && pytest tests/ -v --ignore=tests/agents/ 2>/dev/null || echo "No existing tests to regress"
```

**Step 5: GLOBAL ROLL-09 VALIDATION**

This is the final gate for the entire Phase 4. Run all contract tests across all packages and compute the global multi-category ratio:

```bash
# Run all contract tests from every package
for pkg in lobster-transcriptomics lobster-structural-viz lobster-metabolomics lobster-metadata lobster-genomics lobster-visualization lobster-ml lobster-proteomics lobster-research lobster-drug-discovery; do
  echo "=== $pkg ==="
  cd /path/to/lobster/packages/$pkg && pytest -m contract tests/agents/ -v --timeout=120 2>&1 | tail -5
done

# Also run root-level contract tests (enforcement tests)
cd /path/to/lobster && python -m pytest -m contract tests/ --no-cov -v --timeout=120 2>&1 | tail -10
```

Then compute multi-category ratio:
```python
# Scan all tool files for multi-category metadata
import re, glob
multi = 0
total = 0
for pattern in ['packages/*/lobster/agents/**/*.py', 'lobster/agents/**/*.py']:
    for path in glob.glob(pattern, recursive=True):
        with open(path) as f:
            src = f.read()
        for match in re.findall(r'\.metadata\s*=\s*\{.*?"categories":\s*\[(.*?)\]', src):
            total += 1
            if match.count('"') > 2:  # More than 1 category string
                multi += 1
ratio = (multi / total * 100) if total > 0 else 0
print(f"Multi-category: {multi}/{total} = {ratio:.1f}%")
assert ratio < 40, f"Multi-category ratio {ratio:.1f}% exceeds 40% cap!"
```

If multi-category ratio exceeds 40%, review and simplify multi-category assignments using the 80% rule.

**Step 6: Document results in SUMMARY**

Include in the SUMMARY:
- Drug-discovery per-file mapping tables
- Global statistics: total tools tagged, multi-category ratio, contract test pass count
- Per-package summary table showing tool counts and contract test status
  </action>
  <verify>
    <automated>cd /Users/tyo/Omics-OS/lobster && python -c "
import re, glob

# Count multi-category tools globally
multi = 0
total = 0
for pattern in ['packages/*/lobster/agents/**/*.py']:
    for path in glob.glob(pattern, recursive=True):
        if '__pycache__' in path or '__init__' in path:
            continue
        with open(path) as f:
            src = f.read()
        for match in re.findall(r'\.metadata\s*=\s*\{.*?\"categories\":\s*\[(.*?)\]', src):
            total += 1
            cats = [c.strip().strip('\"') for c in match.split(',')]
            if len(cats) > 1:
                multi += 1
ratio = (multi / total * 100) if total > 0 else 0
print(f'Global multi-category: {multi}/{total} = {ratio:.1f}%')
print('PASS' if ratio < 40 else 'FAIL: exceeds 40%')
"</automated>
  </verify>
  <done>
Drug-discovery contract tests pass for all 4 agents. Global ROLL-09 validation complete: all ~222 tools across 10 packages pass contract tests, multi-category usage is under 40%. Phase 4 agent rollout is complete.
  </done>
</task>

</tasks>

<verification>
1. `cd packages/lobster-drug-discovery && pytest -m contract tests/agents/ -v` — drug-discovery contract tests pass
2. Global multi-category ratio computation returns < 40%
3. All 10 package contract test suites pass (run from each package directory)
4. `grep "ROLL-10" .planning/REQUIREMENTS.md` — ROLL-10 exists in requirements
</verification>

<success_criteria>
- 35/35 drug-discovery tools have correct .metadata and .tags
- drug_discovery_expert passes minimum viable parent check
- Contract tests pass for all 4 drug-discovery agents
- ROLL-10 added to REQUIREMENTS.md
- Global multi-category ratio < 40% (ROLL-09)
- All ~222 tools across all 10 packages have AQUADIF metadata
- Phase 4 is complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-rollout/04-07-SUMMARY.md`

SUMMARY includes:
- Drug-discovery per-file mapping tables
- Global rollout statistics (total tools, multi-category ratio, per-package breakdown)
- Final Phase 4 status (all ROLL requirements met)
</output>
