---
phase: 06-documentation-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/tyo/Omics-OS/CLAUDE.md
  - /Users/tyo/Omics-OS/lobster/CLAUDE.md
  - /Users/tyo/Omics-OS/lobster/.github/CLAUDE.md
  - /Users/tyo/Omics-OS/master_mermaid.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-06]

must_haves:
  truths:
    - "Root CLAUDE.md mentions AQUADIF taxonomy in its lobster/ description"
    - "lobster/CLAUDE.md documents AquadifMonitor in AQUADIF section and Key files table"
    - ".github/CLAUDE.md Hard Rules include AQUADIF metadata requirement (rule 11) and contract test requirement (rule 12)"
    - ".github/CLAUDE.md agent table includes all 22 agents including drug-discovery package"
    - "master_mermaid.md has section 12 with AQUADIF metadata flow Mermaid diagram"
  artifacts:
    - path: "/Users/tyo/Omics-OS/CLAUDE.md"
      provides: "Root monorepo CLAUDE.md with AQUADIF mention"
      contains: "AQUADIF"
    - path: "/Users/tyo/Omics-OS/lobster/CLAUDE.md"
      provides: "Lobster CLAUDE.md with AquadifMonitor documentation"
      contains: "AquadifMonitor"
    - path: "/Users/tyo/Omics-OS/lobster/.github/CLAUDE.md"
      provides: "GitHub contributor guide with AQUADIF rules"
      contains: "AQUADIF metadata"
    - path: "/Users/tyo/Omics-OS/master_mermaid.md"
      provides: "Architecture diagram with AQUADIF layer"
      contains: "AQUADIF Metadata Layer"
  key_links:
    - from: "/Users/tyo/Omics-OS/lobster/CLAUDE.md"
      to: "skills/lobster-dev/references/aquadif-contract.md"
      via: "reference link"
      pattern: "aquadif-contract\\.md"
    - from: "/Users/tyo/Omics-OS/lobster/.github/CLAUDE.md"
      to: "skills/lobster-dev/references/aquadif-contract.md"
      via: "reference link"
      pattern: "aquadif-contract\\.md"
---

<objective>
Update internal documentation files (CLAUDE.md files + master_mermaid.md) to reflect AQUADIF as a shipped, validated system with runtime monitoring.

Purpose: Future contributors and AI agents reading these files learn AQUADIF rules immediately without being told. The architecture diagram becomes the visual reference for the full AQUADIF layer.

Output: 4 updated markdown files — root CLAUDE.md, lobster/CLAUDE.md, .github/CLAUDE.md, master_mermaid.md
</objective>

<execution_context>
@/Users/tyo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tyo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation-release/06-RESEARCH.md

<interfaces>
<!-- Current state of target files — executor needs to understand where to insert content -->

From /Users/tyo/Omics-OS/CLAUDE.md:
- Has "This Workspace" section with Directory Index table (lobster/ row mentions "Core engine" but not AQUADIF)
- Has "Critical Rules" section (6 items) — AQUADIF is mentioned only in the lobster/CLAUDE.md content loaded separately
- The root CLAUDE.md already has a full "AQUADIF Tool Taxonomy" section under the lobster/CLAUDE.md content block (lines 131-163). The ROOT file itself does not have AQUADIF in its own sections.

From /Users/tyo/Omics-OS/lobster/CLAUDE.md:
- "AQUADIF Tool Taxonomy" section exists (lines 131-163) — has 10 categories, assignment pattern, key rules, key files
- "Key files" list (lines 289-299) — includes config/aquadif.py, testing/contract_mixins.py, scaffold/ but NOT core/aquadif_monitor.py
- Missing: AquadifMonitor description, callback chain wiring summary, monitoring infrastructure context

From /Users/tyo/Omics-OS/lobster/.github/CLAUDE.md:
- "Hard Rules" section has 10 rules (lines 40-49). Rule 10 is "Run make format before committing"
- "Agent Roles" table (lines 89-106) — missing drug_discovery_expert, cheminformatics_expert, clinical_dev_expert, pharmacogenomics_expert, variant_analysis_expert, biomarker_discovery_expert, proteomics_de_analysis_expert, metabolomics_expert, protein_structure_visualization_expert
- "Key Files" table (lines 76-86) — missing config/aquadif.py, testing/contract_mixins.py, core/aquadif_monitor.py
- Package tree (lines 22-32) — missing lobster-metabolomics, lobster-drug-discovery

From /Users/tyo/Omics-OS/master_mermaid.md:
- 11 numbered sections (1 through 11), 620 lines
- No AQUADIF content. Section 12 will be appended at end.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md files with AQUADIF monitoring documentation (DOC-01, DOC-02)</name>
  <files>
    /Users/tyo/Omics-OS/CLAUDE.md
    /Users/tyo/Omics-OS/lobster/CLAUDE.md
    /Users/tyo/Omics-OS/lobster/.github/CLAUDE.md
  </files>
  <action>
**A. Root CLAUDE.md (`/Users/tyo/Omics-OS/CLAUDE.md`):**
Minimal change per research recommendation. In the "This Workspace" > "Directory Index" table, update the lobster/ row's "What" column from "Core engine — Python monorepo with packages/ and skills/ (22 agents, 10 packages)" to "Core engine — Python monorepo with packages/ and skills/ (22 agents, 10 packages, AQUADIF tool taxonomy)". No other changes — lobster/CLAUDE.md is the authoritative AQUADIF reference.

**B. lobster/CLAUDE.md:**
1. In the existing "AQUADIF Tool Taxonomy" section (after the "Key rules" block, before "Key files"), add a "Runtime monitoring" subsection:
```markdown
**Runtime monitoring:** `AquadifMonitor` (`core/aquadif_monitor.py`) tracks category distribution and provenance compliance per session via:
- `record_tool_invocation()` — called from `TokenTrackingCallback.on_tool_start` (single injection point)
- `record_provenance_call()` — called from `DataManagerV2.log_tool_usage` (observes actual IR presence)
- `get_session_summary()` — structured event for Omics-OS Cloud SSE enrichment
- Provenance status: `real_ir` / `hollow_ir` / `missing` — ir=None tools tracked as hollow, not violations
- Thread-safe (stdlib only), fail-open, bounded data structures (`deque(maxlen=100)`)

**Contract enforcement:** `pytest -m contract` validates all 221 tools across 21 agents. CI runs on every push/PR.
```

2. In the "Key files" list (around line 296), add after the `scaffold/` entry:
```markdown
- `core/aquadif_monitor.py` – `AquadifMonitor` runtime monitoring (category distribution, provenance compliance, CODE_EXEC log)
```

**C. .github/CLAUDE.md:**
1. After rule 10 ("Run `make format` before committing — black + isort"), add:
```markdown
11. **All tools MUST have AQUADIF metadata** — `.metadata` and `.tags` assigned after `@tool` decorator. Use string literals (`"ANALYZE"`), not enum imports. See `skills/lobster-dev/references/aquadif-contract.md`
12. **PRs adding new tools MUST include contract tests** — run `pytest -m contract` before submitting. CI validates this automatically on every PR
```

2. Update the "Package Architecture" tree (lines 22-32) to include ALL 10 packages:
```
lobster-ai (core SDK)
├── lobster-transcriptomics  # 3 agents (scRNA-seq, annotation, DE)
├── lobster-research         # 2 agents (literature, data loading)
├── lobster-visualization    # 1 agent (plotting)
├── lobster-metadata         # 1 agent (ID mapping, filtering)
├── lobster-structural-viz   # 1 agent (protein structure)
├── lobster-genomics         # 2 agents (genomics, variant analysis)
├── lobster-proteomics       # 3 agents (proteomics, proteomics DE, biomarker discovery)
├── lobster-metabolomics     # 1 agent (LC-MS/GC-MS/NMR)
├── lobster-ml               # 3 agents (ML, feature selection, survival)
└── lobster-drug-discovery   # 4 agents (drug discovery, cheminformatics, clinical dev, pharmacogenomics)
```

3. Update the "Agent Roles" table to include ALL 22 agents (1 supervisor + 21 specialists). Add the missing agents:
- `variant_analysis_expert` | lobster-genomics | Variant annotation, ClinVar, gnomAD
- `proteomics_de_analysis_expert` | lobster-proteomics | Proteomics DE, pathway enrichment, KSEA
- `biomarker_discovery_expert` | lobster-proteomics | Panel selection, nested CV, hub proteins
- `metabolomics_expert` | lobster-metabolomics | LC-MS/GC-MS/NMR analysis
- `protein_structure_visualization_expert` | lobster-structural-viz | PDB fetch, structure visualization
- `drug_discovery_expert` | lobster-drug-discovery | Drug discovery workflows
- `cheminformatics_expert` | lobster-drug-discovery | Molecular descriptors, fingerprints
- `clinical_dev_expert` | lobster-drug-discovery | Clinical trial design, endpoints
- `pharmacogenomics_expert` | lobster-drug-discovery | PGx variants, drug-gene interactions

4. Update the "Key Files" table to add:
```markdown
| `config/aquadif.py` | AQUADIF taxonomy — AquadifCategory enum, provenance rules |
| `testing/contract_mixins.py` | AgentContractTestMixin — 14 contract test methods |
| `core/aquadif_monitor.py` | Runtime AQUADIF monitoring (category + provenance tracking) |
```

**IMPORTANT:** Do NOT restructure existing content. Only ADD sections/rows. Reference `skills/lobster-dev/references/aquadif-contract.md` as authoritative — do not duplicate full rules.
  </action>
  <verify>
    <automated>grep -c "AQUADIF" /Users/tyo/Omics-OS/CLAUDE.md && grep -c "AquadifMonitor" /Users/tyo/Omics-OS/lobster/CLAUDE.md && grep -c "AQUADIF metadata" /Users/tyo/Omics-OS/lobster/.github/CLAUDE.md && grep -c "drug_discovery_expert" /Users/tyo/Omics-OS/lobster/.github/CLAUDE.md</automated>
  </verify>
  <done>
    - Root CLAUDE.md mentions AQUADIF in Directory Index lobster/ row
    - lobster/CLAUDE.md has AquadifMonitor runtime monitoring subsection and core/aquadif_monitor.py in Key files
    - .github/CLAUDE.md has rules 11-12 (AQUADIF metadata + contract tests), all 22 agents in table, all 10 packages in tree, AQUADIF key files added
  </done>
</task>

<task type="auto">
  <name>Task 2: Add AQUADIF Metadata Flow diagram to master_mermaid.md (DOC-06)</name>
  <files>
    /Users/tyo/Omics-OS/master_mermaid.md
  </files>
  <action>
Append section 12 to the end of `/Users/tyo/Omics-OS/master_mermaid.md` (after section 11, which ends around line 620).

The new section must follow the existing numbered pattern (## 12. Title). Use the Mermaid diagram from the research document (06-RESEARCH.md "Code Examples" section) with the following structure:

```markdown
## 12. AQUADIF Metadata Flow & Contract Test Infrastructure

The AQUADIF taxonomy layer (shipped v1.1) makes every tool in Lobster AI introspectable. All 221 tools across 21 agents declare their category and provenance requirements. Contract tests enforce compliance on every CI push. Runtime monitoring tracks category distribution and provenance compliance per session.

[Mermaid diagram from research — the graph TB with 4 subgraphs: TAXONOMY, CONTRACT, RUNTIME, WIRING]
```

Use the exact Mermaid diagram from `06-RESEARCH.md` lines 221-274. It has 4 subgraphs (TAXONOMY, CONTRACT, RUNTIME, WIRING) with correct node labels and color coding.

After the diagram, add a brief prose summary:

```markdown
**Key design decisions:**
- **Path B metadata access:** `graph.py` builds `tool_name -> metadata` lookup dict at construction time. Does NOT rely on LangChain passing metadata through callbacks.
- **Single injection point:** Only `TokenTrackingCallback.on_tool_start` calls the monitor. Display handlers do NOT call it (prevents double-counting in cloud sessions).
- **Provenance by observation:** `DataManagerV2.log_tool_usage` calls `monitor.record_provenance_call()` — observes actual `ir=` presence, not tool output parsing.
- **ir=None whitelist:** 20+ tools pass `ir=None` (v1.0 bridge pattern). Monitor tracks these as `hollow_ir`, not violations.
```
  </action>
  <verify>
    <automated>grep -c "## 12\." /Users/tyo/Omics-OS/master_mermaid.md && grep -c "AQUADIF Metadata Layer" /Users/tyo/Omics-OS/master_mermaid.md</automated>
  </verify>
  <done>
    - master_mermaid.md has section 12 with title "AQUADIF Metadata Flow & Contract Test Infrastructure"
    - Section contains Mermaid graph with 4 subgraphs (TAXONOMY, CONTRACT, RUNTIME, WIRING)
    - Prose summary includes 4 key design decisions
  </done>
</task>

</tasks>

<verification>
All 4 files updated with AQUADIF content:
1. `grep "AQUADIF" /Users/tyo/Omics-OS/CLAUDE.md` — returns matches in Directory Index
2. `grep "AquadifMonitor" /Users/tyo/Omics-OS/lobster/CLAUDE.md` — returns runtime monitoring section
3. `grep "AQUADIF metadata" /Users/tyo/Omics-OS/lobster/.github/CLAUDE.md` — returns rule 11
4. `grep "drug_discovery_expert" /Users/tyo/Omics-OS/lobster/.github/CLAUDE.md` — returns agent table row
5. `grep "## 12" /Users/tyo/Omics-OS/master_mermaid.md` — returns section 12 header
</verification>

<success_criteria>
- Root CLAUDE.md mentions AQUADIF in its own content (not just the lobster/CLAUDE.md copy)
- lobster/CLAUDE.md documents AquadifMonitor with method descriptions and callback chain summary
- .github/CLAUDE.md has 12 Hard Rules (was 10), complete 22-agent table, complete 10-package tree
- master_mermaid.md has 12 sections (was 11) with AQUADIF Mermaid diagram
- No existing content in any file was removed or restructured
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-release/06-01-SUMMARY.md`
</output>
