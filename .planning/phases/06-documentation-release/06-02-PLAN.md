---
phase: 06-documentation-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/tyo/Omics-OS/docs-site/content/docs/extending/aquadif-taxonomy.mdx
  - /Users/tyo/Omics-OS/docs-site/content/docs/extending/meta.json
  - /Users/tyo/Omics-OS/lobster/skills/lobster-dev/references/aquadif-contract.md
  - /Users/tyo/Omics-OS/lobster/skills/lobster-dev/references/testing.md
  - /Users/tyo/Omics-OS/lobster/skills/lobster-use/SKILL.md
autonomous: true
requirements: [DOC-03, DOC-04, DOC-05]

must_haves:
  truths:
    - "docs-site has an AQUADIF page at extending/aquadif-taxonomy visible in sidebar navigation"
    - "AQUADIF page explains all 10 categories with provenance requirements"
    - "lobster-dev aquadif-contract.md mentions AquadifMonitor runtime monitoring"
    - "lobster-dev testing.md references pytest -m contract command"
    - "lobster-use SKILL.md mentions AQUADIF categories in agent system context"
    - "docs-site builds successfully with the new MDX page"
  artifacts:
    - path: "/Users/tyo/Omics-OS/docs-site/content/docs/extending/aquadif-taxonomy.mdx"
      provides: "Public-facing AQUADIF documentation page"
      contains: "AQUADIF Tool Taxonomy"
    - path: "/Users/tyo/Omics-OS/docs-site/content/docs/extending/meta.json"
      provides: "Sidebar navigation including aquadif-taxonomy"
      contains: "aquadif-taxonomy"
    - path: "/Users/tyo/Omics-OS/lobster/skills/lobster-dev/references/aquadif-contract.md"
      provides: "Updated AQUADIF contract with monitoring section"
      contains: "AquadifMonitor"
    - path: "/Users/tyo/Omics-OS/lobster/skills/lobster-use/SKILL.md"
      provides: "End-user skill with AQUADIF context"
      contains: "AQUADIF"
  key_links:
    - from: "/Users/tyo/Omics-OS/docs-site/content/docs/extending/aquadif-taxonomy.mdx"
      to: "/Users/tyo/Omics-OS/docs-site/content/docs/extending/testing.mdx"
      via: "cross-reference link"
      pattern: "testing"
    - from: "/Users/tyo/Omics-OS/docs-site/content/docs/extending/meta.json"
      to: "/Users/tyo/Omics-OS/docs-site/content/docs/extending/aquadif-taxonomy.mdx"
      via: "pages array entry"
      pattern: "aquadif-taxonomy"
---

<objective>
Create public AQUADIF documentation page on docs-site and update skill references to reflect AQUADIF as a shipped system with runtime monitoring.

Purpose: External plugin developers get a docs-site page explaining the taxonomy. Coding agents loading lobster-dev or lobster-use skills get current information about AQUADIF and monitoring.

Output: 1 new MDX page + 1 updated meta.json + 2 updated lobster-dev references + 1 updated lobster-use SKILL.md
</objective>

<execution_context>
@/Users/tyo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tyo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation-release/06-RESEARCH.md

<interfaces>
<!-- Current state of target files for executor reference -->

From docs-site/content/docs/extending/meta.json:
```json
{
  "title": "Extending Lobster",
  "description": "Create custom agent packages",
  "icon": "Puzzle",
  "pages": [
    "plugin-contract",
    "package-structure",
    "entry-points",
    "testing"
  ]
}
```

From docs-site/content/docs/extending/plugin-contract.mdx (frontmatter pattern to follow):
```yaml
---
title: Plugin Contract
description: API contract for agent packages in the Lobster ecosystem
tier: free
---
```

From skills/lobster-dev/references/aquadif-contract.md:
- 632-line comprehensive reference covering: 10 categories, boundary cases, metadata assignment, contract test methods, migration checklist
- Does NOT mention AquadifMonitor or runtime monitoring (Phase 5 completed after this file was last updated)
- Has "Contract Tests" section that covers AgentContractTestMixin but not the `pytest -m contract` CI command

From skills/lobster-dev/references/testing.md:
- Covers test structure, fixtures, mocking patterns, CI commands
- May not link to `pytest -m contract` as a specific command pattern
- Should cross-reference aquadif-contract.md for AQUADIF-specific testing

From skills/lobster-use/SKILL.md:
- End-user focused, no AQUADIF content
- "Agent System" section (line ~217) has agent table — good location for a brief AQUADIF mention
- Users don't need full taxonomy details, just awareness that tools are categorized
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs-site AQUADIF taxonomy page and update navigation (DOC-03)</name>
  <files>
    /Users/tyo/Omics-OS/docs-site/content/docs/extending/aquadif-taxonomy.mdx
    /Users/tyo/Omics-OS/docs-site/content/docs/extending/meta.json
  </files>
  <action>
**A. Create `/Users/tyo/Omics-OS/docs-site/content/docs/extending/aquadif-taxonomy.mdx`:**

Use the MDX template from 06-RESEARCH.md as the starting point. The page must have:

1. **Frontmatter** (copy pattern from plugin-contract.mdx):
```yaml
---
title: AQUADIF Tool Taxonomy
description: 10-category taxonomy for classifying Lobster AI tools — making the system introspectable, enforceable, and teachable
tier: free
---
```

2. **Introduction** (2-3 paragraphs): What AQUADIF is, why it exists, the core principle (every tool declares what it does and whether it produces provenance).

3. **Quick Reference table** with all 10 categories: IMPORT, QUALITY, FILTER, PREPROCESS, ANALYZE, ANNOTATE, DELEGATE, SYNTHESIZE, UTILITY, CODE_EXEC. Columns: Category | Definition | Provenance Required | Example tools.

4. **Metadata Assignment Pattern** section: Show the `@tool` + `.metadata` + `.tags` pattern with a real example. Include the 3 key rules: max 3 categories, string literals only, .metadata and .tags must match.

5. **Provenance Rules** section: Explain which 7 categories require provenance, which 3 do not. Explain `ir=None` (hollow provenance) vs real IR vs missing. Explain the `log_tool_usage(ir=ir)` requirement.

6. **Contract Testing** section: Explain `AgentContractTestMixin` (14 methods), `pytest -m contract`, CI enforcement. Link to the [Testing](/docs/extending/testing) page for setup details.

7. **Runtime Monitoring** section: Briefly describe `AquadifMonitor` — what it tracks (category distribution, provenance status, CODE_EXEC log), how it's wired (TokenTrackingCallback single injection point), `get_session_summary()` for cloud SSE.

8. **Category Decision Guide** section: Help developers choose the right category. Include boundary cases: "80% rule" (if 80% of the tool's logic is X, category is X), UTILITY for read-only tools, DELEGATE for inter-agent handoffs.

**Content source:** Adapt from `skills/lobster-dev/references/aquadif-contract.md` (the authoritative reference). Simplify for a public docs audience — less implementation detail, more "what you need to know to build a plugin."

**MDX rules:**
- Do NOT use `<Callout>` components that are not imported (check if Fumadocs provides them natively — the plugin-contract.mdx uses `<Callout type="info">` so they are available)
- Use standard markdown headings, tables, and code blocks
- Do NOT use JSX components unless they are already used in other extending/ pages

**B. Update `/Users/tyo/Omics-OS/docs-site/content/docs/extending/meta.json`:**

Add `"aquadif-taxonomy"` to the `pages` array. Place it after `"testing"` (last position — it's a reference page, not a getting-started page):

```json
{
  "title": "Extending Lobster",
  "description": "Create custom agent packages",
  "icon": "Puzzle",
  "pages": [
    "plugin-contract",
    "package-structure",
    "entry-points",
    "testing",
    "aquadif-taxonomy"
  ]
}
```

**C. Validate the docs-site build:**

After creating the files, run:
```bash
cd /Users/tyo/Omics-OS/docs-site && npm run build
```

If the build fails, fix the MDX syntax errors. Common issues: missing frontmatter fields, unescaped special characters in MDX, incorrect JSX component usage.
  </action>
  <verify>
    <automated>cd /Users/tyo/Omics-OS/docs-site && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `aquadif-taxonomy.mdx` exists in `docs-site/content/docs/extending/` with correct frontmatter
    - `meta.json` pages array includes "aquadif-taxonomy"
    - `npm run build` succeeds without errors
    - Page covers: 10 categories, metadata pattern, provenance rules, contract testing, runtime monitoring, category decision guide
  </done>
</task>

<task type="auto">
  <name>Task 2: Update lobster-dev and lobster-use skill references (DOC-04, DOC-05)</name>
  <files>
    /Users/tyo/Omics-OS/lobster/skills/lobster-dev/references/aquadif-contract.md
    /Users/tyo/Omics-OS/lobster/skills/lobster-dev/references/testing.md
    /Users/tyo/Omics-OS/lobster/skills/lobster-use/SKILL.md
  </files>
  <action>
**A. Update `skills/lobster-dev/references/aquadif-contract.md` (DOC-04):**

Add a new "## Runtime Monitoring" section AFTER the existing "Contract Tests" section (near the end of the file, before the Migration appendix if present). Content:

```markdown
## Runtime Monitoring

The `AquadifMonitor` service (`lobster/core/aquadif_monitor.py`) tracks AQUADIF compliance at runtime. It is wired into the callback chain and requires zero configuration from plugin authors — your tools are automatically monitored once metadata is assigned.

**What it tracks:**
- **Category distribution** — per-session count of tool invocations by category (`get_category_distribution()`)
- **Provenance status** — per-tool status: `real_ir` (genuine AnalysisStep), `hollow_ir` (ir=None bridge), `missing` (no provenance call observed) (`get_provenance_status()`)
- **CODE_EXEC log** — bounded circular buffer (100 entries) of custom code executions with agent attribution (`get_code_exec_log()`)
- **Session summary** — structured dict consumable by Omics-OS Cloud SSE enrichment (`get_session_summary()`)

**How it's wired:**
1. `client.py` constructs `AquadifMonitor(tool_metadata_map={})`
2. `graph.py` builds `tool_name -> {categories, provenance}` lookup dict from all agent tools and populates the monitor's map
3. `TokenTrackingCallback.on_tool_start` calls `monitor.record_tool_invocation()` (single injection point — no other handlers call monitor)
4. `DataManagerV2.log_tool_usage` calls `monitor.record_provenance_call(tool_name, has_real_ir)` — provenance detection by observation

**Design properties:** Pure stdlib (threading, collections.deque), fail-open (monitor errors never crash tool invocations), thread-safe, bounded data structures.

**For plugin authors:** You do NOT need to interact with AquadifMonitor directly. Assign `.metadata` and `.tags` to your tools, and the monitor will track them automatically.
```

**B. Update `skills/lobster-dev/references/testing.md` (DOC-04):**

Find the section that covers test commands or CI. Add or update to include a clear mention of the contract test marker:

If a "Contract Tests" or "AQUADIF Tests" section does not exist, add one:

```markdown
## AQUADIF Contract Tests

All agents are validated for AQUADIF compliance using `AgentContractTestMixin` (14 methods). These run on every CI push/PR.

**Run contract tests locally:**
```bash
pytest -m contract                      # All agents
pytest -m contract -k transcriptomics   # Single package
```

**Full reference:** See `aquadif-contract.md` for contract test method descriptions, factory setup patterns, and LLM mock configuration.
```

If the section already exists with some of this content, ensure `pytest -m contract` is explicitly shown as a command.

**C. Update `skills/lobster-use/SKILL.md` (DOC-05):**

In the "Agent System" section (around line 217), add a brief paragraph AFTER the agent table and BEFORE "Details and hierarchy":

```markdown
Each agent's tools are organized by the **AQUADIF taxonomy** — 10 categories (IMPORT, QUALITY, FILTER, PREPROCESS, ANALYZE, ANNOTATE, DELEGATE, SYNTHESIZE, UTILITY, CODE_EXEC) that describe what each tool does. This internal organization helps Lobster route your requests to the right tools and track provenance for reproducibility. You don't need to know the categories — just describe what you want in natural language.
```

This is the ONLY change to lobster-use — keep it lightweight. End users don't need category details.

**IMPORTANT for all skill file edits:**
- Do NOT restructure existing content
- Only ADD new sections/paragraphs
- Keep aquadif-contract.md as the authoritative source — testing.md and SKILL.md point to it
  </action>
  <verify>
    <automated>grep -c "AquadifMonitor" /Users/tyo/Omics-OS/lobster/skills/lobster-dev/references/aquadif-contract.md && grep -c "pytest -m contract" /Users/tyo/Omics-OS/lobster/skills/lobster-dev/references/testing.md && grep -c "AQUADIF" /Users/tyo/Omics-OS/lobster/skills/lobster-use/SKILL.md</automated>
  </verify>
  <done>
    - aquadif-contract.md has "Runtime Monitoring" section mentioning AquadifMonitor with 4 tracked metrics and 4 wiring steps
    - testing.md has contract test section with `pytest -m contract` command shown explicitly
    - lobster-use SKILL.md has 1 paragraph in Agent System section mentioning AQUADIF categories
    - No existing content removed from any skill file
  </done>
</task>

</tasks>

<verification>
1. `ls /Users/tyo/Omics-OS/docs-site/content/docs/extending/aquadif-taxonomy.mdx` — file exists
2. `grep "aquadif-taxonomy" /Users/tyo/Omics-OS/docs-site/content/docs/extending/meta.json` — in pages array
3. `cd /Users/tyo/Omics-OS/docs-site && npm run build` — succeeds
4. `grep "AquadifMonitor" /Users/tyo/Omics-OS/lobster/skills/lobster-dev/references/aquadif-contract.md` — found in Runtime Monitoring section
5. `grep "pytest -m contract" /Users/tyo/Omics-OS/lobster/skills/lobster-dev/references/testing.md` — command shown
6. `grep "AQUADIF" /Users/tyo/Omics-OS/lobster/skills/lobster-use/SKILL.md` — mention in Agent System section
</verification>

<success_criteria>
- docs-site builds successfully with new AQUADIF page visible in extending/ sidebar
- AQUADIF page covers all 10 categories, metadata pattern, provenance rules, contract testing, and runtime monitoring
- lobster-dev aquadif-contract.md has Runtime Monitoring section documenting the full callback chain wiring
- lobster-dev testing.md explicitly shows `pytest -m contract` as a runnable command
- lobster-use SKILL.md has a lightweight AQUADIF mention in the Agent System section
- No existing content removed or restructured in any file
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-release/06-02-SUMMARY.md`
</output>
