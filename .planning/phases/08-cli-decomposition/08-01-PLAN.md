---
phase: 08-cli-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/cli/test_cli_decomposition.py
  - lobster/cli_internal/commands/heavy/session_infra.py
  - lobster/cli_internal/commands/heavy/animations.py
  - lobster/cli_internal/commands/heavy/display_helpers.py
  - lobster/cli_internal/commands/heavy/init_commands.py
  - lobster/cli_internal/commands/heavy/chat_commands.py
  - lobster/cli_internal/commands/heavy/query_commands.py
autonomous: true
requirements:
  - CLID-01
must_haves:
  truths:
    - "Command implementation bodies for init, chat, query live in cli_internal/commands/heavy/ as importable modules"
    - "Session infrastructure (LobsterClientAdapter, CommandClient, init_client, history, progress) is importable from session_infra"
    - "Animations (DNA helix, agent loading, exit) are importable from animations module"
    - "Display helpers (data preview, status info) are importable from display_helpers module"
  artifacts:
    - path: "lobster/cli_internal/commands/heavy/session_infra.py"
      provides: "Shared session infrastructure (client init, adapters, history, progress)"
      min_lines: 500
    - path: "lobster/cli_internal/commands/heavy/animations.py"
      provides: "Terminal DNA helix and agent loading animations"
      min_lines: 300
    - path: "lobster/cli_internal/commands/heavy/display_helpers.py"
      provides: "Data preview formatting and status display"
      min_lines: 200
    - path: "lobster/cli_internal/commands/heavy/init_commands.py"
      provides: "init command body and 17 helper functions"
      min_lines: 1400
    - path: "lobster/cli_internal/commands/heavy/chat_commands.py"
      provides: "chat command body and supporting functions"
      min_lines: 800
    - path: "lobster/cli_internal/commands/heavy/query_commands.py"
      provides: "query command body"
      min_lines: 200
    - path: "tests/unit/cli/test_cli_decomposition.py"
      provides: "Structural tests for decomposition verification"
      min_lines: 30
  key_links:
    - from: "lobster/cli_internal/commands/heavy/chat_commands.py"
      to: "lobster/cli_internal/commands/heavy/session_infra.py"
      via: "import init_client_with_animation, LobsterClientAdapter"
      pattern: "from.*session_infra import"
    - from: "lobster/cli_internal/commands/heavy/query_commands.py"
      to: "lobster/cli_internal/commands/heavy/session_infra.py"
      via: "import init_client, _display_streaming_response"
      pattern: "from.*session_infra import"
---

<objective>
Extract command implementation bodies from cli.py into focused modules under cli_internal/commands/heavy/.

Purpose: Move the "big four" command bodies (init, chat, query) and their shared infrastructure (session management, animations, display) out of the 9,323-line cli.py monolith. This is the foundation layer -- slash commands and final wiring happen in Plan 02.

Output: 6 new modules in cli_internal/commands/heavy/ plus structural test scaffold.
</objective>

<execution_context>
@/Users/tyo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tyo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cli-decomposition/08-RESEARCH.md
@.planning/phases/08-cli-decomposition/08-VALIDATION.md

@lobster/cli.py
@lobster/cli_internal/commands/__init__.py
@lobster/cli_internal/commands/heavy/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create structural test scaffold and extract foundation modules (session_infra, animations, display_helpers)</name>
  <files>
    tests/unit/cli/test_cli_decomposition.py,
    lobster/cli_internal/commands/heavy/session_infra.py,
    lobster/cli_internal/commands/heavy/animations.py,
    lobster/cli_internal/commands/heavy/display_helpers.py
  </files>
  <action>
**Wave 0 -- Test scaffold:**

Create `tests/unit/cli/test_cli_decomposition.py` with structural tests:
- `test_heavy_modules_importable()` -- verify all 6 new heavy modules can be imported without error: session_infra, animations, display_helpers, init_commands, chat_commands, query_commands. Use `importlib.import_module()`. Mark as xfail initially (modules don't exist yet).
- `test_session_infra_exports()` -- verify session_infra exports: `LobsterClientAdapter`, `CloudAwareCache`, `CommandClient`, `NoOpProgress`, `should_show_progress`, `create_progress`, `init_client`, `init_client_with_animation`. Mark xfail.
- `test_animations_exports()` -- verify animations exports: `_dna_helix_animation`, `_dna_agent_loading_phase`, `_dna_exit_animation`. Mark xfail.

**Foundation modules -- extract from cli.py:**

1. **session_infra.py** (~800 LOC): Extract these functions/classes from cli.py:
   - `LobsterClientAdapter` class (line ~317)
   - `CloudAwareCache` class (line ~381)
   - `CommandClient` class (line ~568)
   - `NoOpProgress` class (line ~218)
   - `should_show_progress()` (line ~240)
   - `create_progress()` (line ~281)
   - `_add_command_to_history()` (line ~423)
   - `_backup_command_to_file()` (line ~510)
   - `_str_to_bool()` (line ~1780)
   - `_resolve_profile_timings_flag()` (line ~1791)
   - `_collect_profile_timings()` (line ~1798)
   - `_maybe_print_timings()` (line ~1810)
   - `_get_extraction_cache_manager()` (line ~159)
   - `init_client()` (line ~1832) -- 343 LOC, the big client initialization function
   - `init_client_with_animation()` (line ~3654)
   - `display_session()` (line ~3523)

   Use `from lobster.ui.console_manager import get_console_manager` for console access (matching existing pattern in agent_commands.py). Keep all original imports needed by these functions. Module-level: `console_manager = get_console_manager(); console = console_manager.console`.

2. **animations.py** (~500 LOC): Extract:
   - `_dna_helix_animation()` (line ~2691, 134 LOC)
   - `_dna_agent_loading_phase()` (line ~2835, 151 LOC)
   - `_dna_exit_animation()` (line ~2986, 140 LOC)

   These are self-contained -- they only need `console` and `rich` imports.

3. **display_helpers.py** (~300 LOC): Extract:
   - `_format_data_preview()` (line ~3195)
   - `_format_dataframe_preview()` (line ~3242)
   - `_format_array_info()` (line ~3292)
   - `_get_matrix_info()` (line ~3311)
   - `_display_status_info()` (line ~3336, 187 LOC)

   These are self-contained -- they only need `console` and `rich` imports.

**After extraction:** In cli.py, replace each extracted function body with an import and delegation to the new module. For example:
```python
def init_client(...):
    from lobster.cli_internal.commands.heavy.session_infra import init_client as _init_client
    return _init_client(...)
```

This keeps cli.py working during the incremental extraction. Plan 02 will convert these to thin wrappers.

**IMPORTANT:** Do NOT remove the function signatures from cli.py yet -- other functions still in cli.py call them. Only replace the BODY with a delegation call. The function signature and Typer decorators (if any) stay.

**After creating the modules:** Remove xfail marks from the structural tests that now pass.
  </action>
  <verify>
    <automated>cd /Users/tyo/Omics-OS/lobster && python -m pytest tests/unit/cli/test_cli_decomposition.py -x --no-cov -q</automated>
  </verify>
  <done>
    - session_infra.py, animations.py, display_helpers.py exist in cli_internal/commands/heavy/
    - All 3 modules are importable
    - Functions in cli.py delegate to the new modules (bodies replaced with import + call)
    - Structural tests pass (non-xfail ones)
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract init, chat, and query command bodies</name>
  <files>
    lobster/cli_internal/commands/heavy/init_commands.py,
    lobster/cli_internal/commands/heavy/chat_commands.py,
    lobster/cli_internal/commands/heavy/query_commands.py,
    lobster/cli.py
  </files>
  <action>
**Extract the three major command bodies:**

1. **init_commands.py** (~1,800 LOC): Extract from cli.py:
   - `init()` body (line ~4898, 1,355 LOC) as `init_impl()`
   - `_create_workspace_config()` (line ~4378)
   - `_create_global_config()` (line ~4434)
   - `_uv_tool_env_handoff()` (line ~4510)
   - `_ensure_provider_installed()` (line ~4614)
   - `_prompt_docling_install()` (line ~4649)
   - `_download_ontology_databases()` (line ~4691)
   - `_prompt_smart_standardization()` (line ~4737)
   - `_install_extended_data()` (line ~4851)
   - `_ensure_tui_installed()` (line ~4880)
   - `_get_installed_agents()` (line ~1269)
   - `_display_agent_selection_list()` (line ~1276)
   - `_prompt_manual_agent_selection()` (line ~1307)
   - `_prompt_automatic_agent_selection()` (line ~1414)
   - `_check_and_prompt_install_packages()` (line ~1477)
   - `_save_agent_config()` (line ~1556)
   - `_perform_agent_selection_non_interactive()` (line ~1569)
   - `_perform_agent_selection_interactive()` (line ~1652)

   Use `from lobster.ui.console_manager import get_console_manager` for console. Import `component_registry` lazily inside functions (not at module level, per Hard Rule #11).

2. **chat_commands.py** (~1,200 LOC): Extract:
   - `chat()` body (line ~6332, 368 LOC) as `chat_impl()`
   - `get_user_input_with_editing()` (line ~2175)
   - `_display_streaming_response()` (line ~6253)
   - `display_welcome()` (line ~2825)
   - `display_goodbye()` (line ~3126)
   - `_show_workspace_prompt()` (line ~3550)
   - `change_mode()` (line ~1188)
   - `get_current_agent_name()` (line ~2666)
   - `handle_command()` (line ~6700)

   Import from session_infra: `init_client_with_animation`, `LobsterClientAdapter`, `display_session`.
   Import from animations: `_dna_helix_animation`, `_dna_agent_loading_phase`, `_dna_exit_animation`.

3. **query_commands.py** (~300 LOC): Extract:
   - `query()` body (line ~7784, 263 LOC) as `query_impl()`

   Import from session_infra: `init_client`, `_display_streaming_response` (or from chat_commands if that's where streaming lives -- check dependency direction; if _display_streaming_response is used by both chat and query, put it in session_infra).

**In cli.py:** Replace each extracted command body with delegation:
```python
@app.command()
def init(global_config: ..., force: ..., ...):
    """Initialize Lobster AI configuration."""
    from lobster.cli_internal.commands.heavy.init_commands import init_impl
    init_impl(global_config=global_config, force=force, ...)
```

Keep the `@app.command()` decorator and all `typer.Option()` parameter declarations in cli.py. Only the function BODY is replaced with a single import+call.

**Update test scaffold:** Remove remaining xfail marks from test_cli_decomposition.py for modules that now exist. Add a test verifying `init_impl`, `chat_impl`, `query_impl` are callable.

**Run full CLI test suite after extraction to catch regressions.**
  </action>
  <verify>
    <automated>cd /Users/tyo/Omics-OS/lobster && python -m pytest tests/unit/cli/ -x --no-cov -q</automated>
  </verify>
  <done>
    - init_commands.py, chat_commands.py, query_commands.py exist in cli_internal/commands/heavy/
    - All 3 modules are importable with _impl functions callable
    - cli.py init/chat/query commands delegate to the new modules
    - All existing CLI tests still pass
  </done>
</task>

</tasks>

<verification>
- All 6 new heavy modules importable: `python -c "from lobster.cli_internal.commands.heavy import session_infra, animations, display_helpers, init_commands, chat_commands, query_commands"`
- Existing CLI tests pass: `cd /Users/tyo/Omics-OS/lobster && python -m pytest tests/unit/cli/ -x --no-cov -q`
- Structural tests pass: `cd /Users/tyo/Omics-OS/lobster && python -m pytest tests/unit/cli/test_cli_decomposition.py -x --no-cov -q`
</verification>

<success_criteria>
- 6 new modules in cli_internal/commands/heavy/ containing extracted command bodies
- cli.py command functions delegate to new modules (body replaced with import+call)
- All existing CLI tests pass without modification (patch paths still work via delegation shims)
- Structural test scaffold exists and passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-cli-decomposition/08-01-SUMMARY.md`
</output>
