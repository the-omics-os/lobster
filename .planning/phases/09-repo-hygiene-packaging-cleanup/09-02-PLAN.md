---
phase: 09-repo-hygiene-packaging-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/test_gse248556_bug_fixes.py
  - tests/unit/tools/test_geo_downloader.py
  - lobster/tools/geo_parser.py
  - lobster/tools/geo_downloader.py
autonomous: true
requirements:
  - HYGN-03
  - HYGN-04

must_haves:
  truths:
    - "No empty placeholder directories remain in lobster/ or tests/"
    - "geo_parser.py shim file is removed (zero importers)"
    - "geo_downloader.py shim file is removed after updating 2 test importers to canonical path"
    - "All tests pass after import path updates"
  artifacts:
    - path: "tests/unit/tools/test_geo_downloader.py"
      provides: "Updated import from canonical path"
      contains: "from lobster.services.data_access.geo.downloader import GEODownloadManager"
    - path: "tests/integration/test_gse248556_bug_fixes.py"
      provides: "Updated import from canonical path"
      contains: "from lobster.services.data_access.geo.downloader import GEODownloadManager"
  key_links:
    - from: "tests/unit/tools/test_geo_downloader.py"
      to: "lobster/services/data_access/geo/downloader.py"
      via: "direct import of GEODownloadManager"
      pattern: "from lobster\\.services\\.data_access\\.geo\\.downloader import"
---

<objective>
Remove empty placeholder directories and deprecated shim files (geo_parser.py, geo_downloader.py) after verifying and updating all importers.

Purpose: Clean up artifacts left from GEO decomposition (Phase 4) -- empty dirs that were never populated, and shim files that are no longer needed now that canonical paths exist.
Output: 13 empty directories removed, 2 deprecated shim files removed, 2 test files updated to canonical imports.
</objective>

<execution_context>
@/Users/tyo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tyo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-repo-hygiene-packaging-cleanup/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove empty placeholder directories</name>
  <files>(directory removals only, no source files modified)</files>
  <action>
Remove these 13 empty directories that were identified in research as never populated and not tracked in git:

```
lobster/.benchmarks/
lobster/data/cache/
lobster/services/visualization/
lobster/services/quality/
lobster/services/data_access/geo/utils/
lobster/services/data_access/geo/sample/
lobster/services/data_access/geo/metadata/
lobster/services/ml/
lobster/services/templates/
lobster/services/metadata/protocol_extraction/mass_spec/resources/
lobster/services/metadata/protocol_extraction/rnaseq/resources/
tests/resilience/
test_output/
```

Before removing each, verify it is actually empty with `ls -la`. If any directory contains files, do NOT remove it -- skip and note.

Use `rmdir` (not `rm -rf`) for safety -- rmdir only removes empty directories.

Do NOT remove any directory that contains a `.gitkeep` file or `__init__.py`.
  </action>
  <verify>
    <automated>cd /Users/tyo/Omics-OS/lobster && find lobster tests -type d -empty -not -path '*__pycache__*' -not -path '*.git*' -not -path '*node_modules*' -not -path '*.venv*' 2>/dev/null | grep -v __pycache__ | wc -l</automated>
  </verify>
  <done>All 13 empty placeholder directories removed. `find` for empty dirs returns 0 (or only __pycache__ dirs which are expected).</done>
</task>

<task type="auto">
  <name>Task 2: Update imports and remove deprecated shim files</name>
  <files>tests/integration/test_gse248556_bug_fixes.py, tests/unit/tools/test_geo_downloader.py, lobster/tools/geo_parser.py, lobster/tools/geo_downloader.py</files>
  <action>
Step 1: Update the 2 test files that import from the deprecated geo_downloader.py shim.

In `tests/unit/tools/test_geo_downloader.py` line 21, change:
```python
from lobster.tools.geo_downloader import GEODownloadManager
```
to:
```python
from lobster.services.data_access.geo.downloader import GEODownloadManager
```

In `tests/integration/test_gse248556_bug_fixes.py` line 482, change:
```python
from lobster.tools.geo_downloader import GEODownloadManager
```
to:
```python
from lobster.services.data_access.geo.downloader import GEODownloadManager
```

Step 2: Run the affected tests to verify imports work:
```bash
pytest tests/unit/tools/test_geo_downloader.py -x --no-cov -q
```

Step 3: After tests pass, remove the deprecated shim files:
```bash
git rm lobster/tools/geo_parser.py
git rm lobster/tools/geo_downloader.py
```

geo_parser.py has ZERO importers (confirmed by grep). geo_downloader.py has 2 importers (just updated above).

Step 4: Run unit tests again to confirm nothing broke:
```bash
pytest tests/unit/ -x --no-cov -q
```
  </action>
  <verify>
    <automated>cd /Users/tyo/Omics-OS/lobster && pytest tests/unit/tools/test_geo_downloader.py -x --no-cov -q && python -c "from lobster.services.data_access.geo.downloader import GEODownloadManager; print('canonical import OK')" && test ! -f lobster/tools/geo_parser.py && echo "geo_parser.py removed" && test ! -f lobster/tools/geo_downloader.py && echo "geo_downloader.py removed"</automated>
  </verify>
  <done>2 test files updated to canonical import path, both deprecated shim files removed, all unit tests pass.</done>
</task>

</tasks>

<verification>
- `find lobster tests -type d -empty -not -path '*__pycache__*' | wc -l` returns 0
- `test ! -f lobster/tools/geo_parser.py` succeeds (file removed)
- `test ! -f lobster/tools/geo_downloader.py` succeeds (file removed)
- `pytest tests/unit/tools/test_geo_downloader.py -x --no-cov -q` passes
- `pytest tests/unit/ -x --no-cov -q` passes (full unit suite)
</verification>

<success_criteria>
- All empty placeholder directories removed (13 total)
- geo_parser.py and geo_downloader.py deprecated shims removed
- 2 test files updated to import from canonical path
- All unit tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/09-repo-hygiene-packaging-cleanup/09-02-SUMMARY.md`
</output>
