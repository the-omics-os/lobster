---
phase: 10-fix-provenance-getattr-shim
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lobster/core/provenance/__init__.py
  - tests/unit/core/test_core_subpackage_shims.py
autonomous: true
requirements:
  - CORE-04
gap_closure: true

must_haves:
  truths:
    - "`from lobster.core.provenance import AnalysisStep` resolves without error"
    - "`from lobster.core.provenance import X` works for all public names in analysis_ir.py, lineage.py, and ir_coverage.py"
    - "Existing `from lobster.core.provenance import ProvenanceTracker` still works with same deprecation warning"
    - "de_analysis_expert.py's 6 call sites can import AnalysisStep without ImportError"
  artifacts:
    - path: "lobster/core/provenance/__init__.py"
      provides: "Multi-module __getattr__ shim searching all 4 submodules"
      contains: "_SUBMODULES"
    - path: "tests/unit/core/test_core_subpackage_shims.py"
      provides: "Extended shim test coverage for AnalysisStep, ParameterSpec, LineageMetadata, IRCoverageAnalyzer"
      contains: "AnalysisStep"
  key_links:
    - from: "lobster/core/provenance/__init__.py"
      to: "lobster/core/provenance/analysis_ir.py"
      via: "__getattr__ importlib.import_module fallthrough"
      pattern: "_SUBMODULES.*analysis_ir"
    - from: "lobster/core/provenance/__init__.py"
      to: "lobster/core/provenance/lineage.py"
      via: "__getattr__ importlib.import_module fallthrough"
      pattern: "_SUBMODULES.*lineage"
---

<objective>
Extend the provenance `__init__.py` `__getattr__` shim to search all 4 submodules (provenance.py, analysis_ir.py, lineage.py, ir_coverage.py), fixing the ImportError that breaks DE analysis provenance IR creation at 6 runtime call sites.

Purpose: Phase 06 created the provenance subpackage but the `__getattr__` shim only delegates to `provenance.py`. Any import of `AnalysisStep` (from `analysis_ir.py`) via the old path fails at runtime. This blocks the DE analysis expert from creating provenance records.

Output: Fixed `__init__.py` with multi-module search, extended test coverage confirming all key names resolve.
</objective>

<execution_context>
@/Users/tyo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tyo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-fix-provenance-getattr-shim/10-RESEARCH.md

@lobster/core/provenance/__init__.py
@tests/unit/core/test_core_subpackage_shims.py

<interfaces>
<!-- Current broken shim in provenance/__init__.py -->
<!-- Only searches provenance.py, misses analysis_ir.py, lineage.py, ir_coverage.py -->

From lobster/core/provenance/analysis_ir.py (key exports needed by DE expert):
```python
class ParameterSpec: ...
class AnalysisStep: ...
def validate_ir_list(ir_list): ...
def create_minimal_ir(tool_name, params, summary): ...
def create_data_loading_ir(tool_name, file_path, data_shape, summary): ...
def create_data_saving_ir(tool_name, file_path, data_shape, summary): ...
```

From lobster/core/provenance/lineage.py (key exports):
```python
class LineageMetadata: ...
def create_lineage_metadata(...): ...
def attach_lineage(adata, metadata): ...
def get_lineage(adata): ...
```

From lobster/core/provenance/ir_coverage.py (key exports):
```python
class ServiceCoverage: ...
class CoverageReport: ...
class IRCoverageAnalyzer: ...
```

From lobster/core/provenance/provenance.py (already works):
```python
class ProvenanceTracker: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add multi-module shim test cases (Wave 0) and extend __getattr__</name>
  <files>tests/unit/core/test_core_subpackage_shims.py, lobster/core/provenance/__init__.py</files>
  <behavior>
    - Test: `from lobster.core.provenance import AnalysisStep` resolves and emits DeprecationWarning mentioning "analysis_ir"
    - Test: `from lobster.core.provenance import ParameterSpec` resolves and emits DeprecationWarning mentioning "analysis_ir"
    - Test: `from lobster.core.provenance import LineageMetadata` resolves and emits DeprecationWarning mentioning "lineage"
    - Test: `from lobster.core.provenance import IRCoverageAnalyzer` resolves and emits DeprecationWarning mentioning "ir_coverage"
    - Test: `from lobster.core.provenance import ProvenanceTracker` still works (existing test unchanged)
    - Test: `from lobster.core.provenance import nonexistent_name` raises AttributeError
  </behavior>
  <action>
RED phase:
1. Add a parametrized test to `test_core_subpackage_shims.py` that covers the provenance package-level `__getattr__` for names from ALL 4 submodules. Add test cases:
   - `("AnalysisStep", "analysis_ir")` -- from analysis_ir.py
   - `("ParameterSpec", "analysis_ir")` -- from analysis_ir.py
   - `("LineageMetadata", "lineage")` -- from lineage.py
   - `("IRCoverageAnalyzer", "ir_coverage")` -- from ir_coverage.py
   - `("ProvenanceTracker", "provenance")` -- existing, must still pass

Each test case should:
  a. Import `lobster.core.provenance` (clear from sys.modules first)
  b. Access the name via `getattr(mod, name)` inside `warnings.catch_warnings(record=True)`
  c. Assert the object is not None
  d. Assert a DeprecationWarning was emitted containing the expected submodule string

Also add a test that `getattr(mod, "nonexistent_xyz_123")` raises `AttributeError`.

2. Run tests -- new AnalysisStep/ParameterSpec/LineageMetadata/IRCoverageAnalyzer tests MUST fail (RED).

GREEN phase:
3. Replace the single-module `__getattr__` in `lobster/core/provenance/__init__.py` with the multi-module version from RESEARCH.md:
   - Define `_SUBMODULES` tuple at module level with all 4 submodule paths (provenance first, then analysis_ir, lineage, ir_coverage)
   - Loop over `_SUBMODULES` in `__getattr__`, `importlib.import_module` each, check `hasattr`, emit DeprecationWarning pointing to the specific submodule, return the attr
   - Raise `AttributeError` if no submodule has the name
   - Keep `stacklevel=2` and warning format compatible with existing tests

4. Run tests -- all MUST pass (GREEN).
  </action>
  <verify>
    <automated>cd /Users/tyo/Omics-OS/lobster && python -m pytest tests/unit/core/test_core_subpackage_shims.py -x -q</automated>
  </verify>
  <done>All provenance shim tests pass: AnalysisStep, ParameterSpec, LineageMetadata, IRCoverageAnalyzer, ProvenanceTracker all resolvable via `from lobster.core.provenance import X` with correct DeprecationWarning. AttributeError raised for unknown names.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke test DE analysis import path and run broader test suite</name>
  <files>tests/unit/core/test_core_subpackage_shims.py</files>
  <action>
1. Run the smoke test that validates de_analysis_expert's import path works:
   ```
   python -c "from lobster.core.provenance import AnalysisStep; print('OK')"
   ```
   This must print "OK" without any ImportError.

2. Run the broader core unit test suite to verify no regressions:
   ```
   python -m pytest tests/unit/core/ -x -q
   ```

3. Verify the existing `test_shim_reexports_and_warns[provenance]` parametrized test (from Phase 06) still passes with the updated `__getattr__`. This test checks that `from lobster.core.provenance import ProvenanceTracker` emits a DeprecationWarning -- it must not be broken by the multi-module change.

4. If any existing tests fail due to warning message format changes, adjust ONLY the test assertions (not the shim) to match the new multi-module warning format. The shim's warning format should stay: `"Import '{name}' from '{mod_path}' instead of 'lobster.core.provenance'. Shim will be removed in v2.0.0."`
  </action>
  <verify>
    <automated>cd /Users/tyo/Omics-OS/lobster && python -c "from lobster.core.provenance import AnalysisStep; print('OK')" && python -m pytest tests/unit/core/ -x -q</automated>
  </verify>
  <done>Smoke test prints "OK", all core unit tests pass, existing ProvenanceTracker shim test still green, no regressions.</done>
</task>

</tasks>

<verification>
1. `python -c "from lobster.core.provenance import AnalysisStep; print('OK')"` prints OK
2. `python -c "from lobster.core.provenance import ProvenanceTracker; print('OK')"` prints OK
3. `python -m pytest tests/unit/core/test_core_subpackage_shims.py -x -q` all pass
4. `python -m pytest tests/unit/core/ -x -q` all pass
</verification>

<success_criteria>
- `from lobster.core.provenance import AnalysisStep` resolves without ImportError
- `from lobster.core.provenance import X` works for key public names from all 4 submodules
- Existing ProvenanceTracker backward-compat shim behavior preserved
- All unit tests in tests/unit/core/ pass
- DE analysis expert's 6 call sites would no longer fail at import time
</success_criteria>

<output>
After completion, create `.planning/phases/10-fix-provenance-getattr-shim/10-01-SUMMARY.md`
</output>
