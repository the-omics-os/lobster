# {{ display_name }} — PEP 420 namespace, state exports only
# Agent discovery happens through entry points in pyproject.toml.
# Do NOT use try/except ImportError here — see creating-agents.md.

from lobster.agents.{{ domain }}.state import {{ state_class }}

__all__ = ["{{ state_class }}"]
